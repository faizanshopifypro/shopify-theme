{% if product.handle == 'vita-abo' %}
  <style>
    .product__info-wrapper variant-radios,
        .product__info-wrapper variant-selects,
        .product__info-wrapper .product-form__submit,
        #Quantity-Form-{{ section.id }} {
          display: none !important;
        }

    .product__info-wrapper .sls-purchase-options-container,
    .sls-purchase-options-container {
      position: absolute !important;
      width: 1px !important;
      height: 1px !important;
      padding: 0 !important;
      margin: -1px !important;
      overflow: hidden !important;
      clip: rect(0, 0, 0, 0) !important;
      white-space: nowrap !important;
      border: 0 !important;
      opacity: 0 !important;
      pointer-events: none !important;
    }
  </style>

  <style>
    :root {
       --color-bg: #ffffff;
       --color-bg-muted: #efe9e2;
       --color-border: #e2ddd5;
       --color-border-active: #1a1a1a;
       --color-text: #1a1a1a;
       --color-text-muted: #5f5a54;
       --color-accent: #cc3f7a;
       --color-accent-bg: #2b2320;
       --color-success: #3f7a4f;
       --color-cta: #6f8f6a;
       --color-cta-hover: #5f7d5a;
       --color-white: #ffffff;
       --radius-sm: 6px;
       --radius-md: 10px;
       --radius-lg: 14px;
       --radius-pill: 999px;
       --space-1: 4px;
       --space-2: 8px;
       --space-3: 12px;
       --space-4: 16px;
       --space-5: 20px;
       --space-6: 24px;
       --fs-xs: 12px;
       --fs-sm: 12px;
       --fs-md: 12px;
       --fs-lg: 15px;
       --fs-xl: 15px;
       --transition-fast: 0.18s ease;
       --transition-med: 0.28s ease;
     }

     * {
       box-sizing: border-box;
       padding: 0;
       margin: 0;
     }

     .purchase {
       max-width: 500px;
       margin: 0 auto;
       display: flex;
       flex-direction: column;
       gap: var(--space-4);
       display: block !important;
     }

     /* Recommendation banner */
     .recommendation {
       background: var(--color-bg-muted);
       border-radius: var(--radius-md);
       padding: var(--space-4);
       font-size: var(--fs-md);
       line-height: 1.5;
     }

     .recommendation__row {
       margin: 0;
     }

     .recommendation__row+.recommendation__row {
       margin-top: var(--space-1);
     }

     /* Dog weight calculator */
     .dog-calculator {
       background: var(--color-bg);
       border: 1px solid var(--color-border);
       border-radius: var(--radius-lg);
       padding: var(--space-4);
       transition: box-shadow var(--transition-med);
     }

     .dog-calculator--highlight {
       box-shadow: 0 0 0 2px var(--color-cta);
     }

     .dog-calculator__title {
       font-size: var(--fs-lg);
       font-weight: 700;
       margin: 0 0 var(--space-2);
     }

     .dog-calculator__question {
       font-size: var(--fs-md);
       color: var(--color-text-muted);
       margin: 0 0 var(--space-4);
     }

     .dog-calculator__controls {
       display: flex;
       align-items: center;
       justify-content: center;
       gap: var(--space-5);
     }

     .dog-calculator__btn {
       width: 44px;
       height: 44px;
       border-radius: var(--radius-md);
       border: 1.5px solid var(--color-border);
       background: var(--color-bg);
       font-size: 1.25rem;
       line-height: 1;
       cursor: pointer;
       font-family: inherit;
       color: var(--color-text);
       display: flex;
       align-items: center;
       justify-content: center;
       transition: border-color var(--transition-fast), background var(--transition-fast);
     }

     .dog-calculator__btn:hover {
       border-color: var(--color-border-active);
       background: var(--color-bg-muted);
     }

     .dog-calculator__btn:focus-visible {
       outline: 2px solid var(--color-border-active);
       outline-offset: 2px;
     }

     .dog-calculator__weight {
       font-size: var(--fs-xl);
       font-weight: 700;
       min-width: 5rem;
       text-align: center;
     }

     /* Fieldset wrapper */
     .purchase-options {
       border: none;
       margin: 0;
       padding: 0;
       display: flex;
       flex-direction: column;
       gap: var(--space-4);
     }

     .purchase-options__legend {
       position: absolute;
       width: 1px;
       height: 1px;
       overflow: hidden;
       clip: rect(0 0 0 0);
     }

     /* Purchase option card */
     .purchase-option {
       border: 1px solid var(--color-border);
       border-radius: var(--radius-lg);
       background: #F7F4EF;
       overflow: hidden;
       transition: border-color var(--transition-med), background var(--transition-med);
     }

     .purchase-option--active {
       background: var(--color-bg);
     }

     .purchase-option__header {
       width: 100%;
       display: flex;
       align-items: center;
       justify-content: space-between;
       gap: var(--space-3);
       padding: var(--space-4);
       background: transparent;
       border: none;
       cursor: pointer;
       font-family: inherit;
       text-align: left;
     }

     .purchase-option__left {
       display: flex;
       align-items: center;
       gap: var(--space-3);
     }

     /* Custom radio */
     .radio {
       width: 22px;
       height: 22px;
       border-radius: 50%;
       border: 2px solid #c9c3ba;
       flex-shrink: 0;
       position: relative;
       transition: border-color var(--transition-fast);
     }

     .radio::after {
       content: "";
       position: absolute;
       inset: 3px;
       border-radius: 50%;
       background: var(--color-border-active);
       transform: scale(0);
       transition: transform var(--transition-fast);
     }

     .purchase-option--active .radio {
       border-color: var(--color-border-active);
     }

     .purchase-option--active .radio::after {
       transform: scale(1);
     }

     .purchase-option__title {
       font-size: var(--fs-lg);
       font-weight: 600;
       margin: 0;
     }

     .purchase-option__price {
       display: flex;
       align-items: baseline;
       gap: var(--space-2);
       font-size: var(--fs-lg);
       font-weight: 700;
       white-space: nowrap;
     }

     .purchase-option__price--original {
       font-size: var(--fs-sm);
       font-weight: 400;
       color: var(--color-text-muted);
       text-decoration: line-through;
     }

     .purchase-option__price--current {
       color: var(--color-text);
     }

     .purchase-option--subscribe .purchase-option__price--current {
       color: var(--color-accent);
     }

     /* Collapsible body */
     .purchase-option__body {
       display: grid;
       grid-template-rows: 0fr;
       transition: grid-template-rows var(--transition-med);
     }

     .purchase-option__body-inner {
       overflow: hidden;
     }

     .purchase-option--active .purchase-option__body {
       grid-template-rows: 1fr;
     }

     .purchase-option__content {
       padding: var(--space-4) var(--space-4);
       display: flex;
       flex-direction: column;
       gap: var(--space-4);
     }

     /* Variant cards */
     .variants {
       display: grid;
       grid-template-columns: repeat(3, 1fr);
       gap: var(--space-2);
     }

     .variant-card {
       position: relative;
       border: 1.5px solid var(--color-border);
       border-radius: var(--radius-md);
       background: var(--color-bg);
       padding: var(--space-3) var(--space-2);
       text-align: center;
       cursor: pointer;
       font-family: inherit;
       display: flex;
       flex-direction: column;
       gap: var(--space-1);
       transition: border-color var(--transition-fast), background var(--transition-fast), color var(--transition-fast);
     }

     .variant-card:hover {
       border-color: var(--color-border-active);
     }

     .variant-card__pack {
       font-size: var(--fs-sm);
       font-weight: 600;
     }

     .variant-card__price {
       font-size: var(--fs-xs);
       color: var(--color-text-muted);
     }

     .variant-card--active {
       background: var(--color-accent-bg);
       border-color: var(--color-accent-bg);
     }

     .variant-card--active .variant-card__pack,
     .variant-card--active .variant-card__price {
       color: var(--color-white);
     }

     .variant-card__badge {
       position: absolute;
       top: -8px;
       right: -6px;
       background: var(--color-accent);
       color: var(--color-white);
       font-size: 10px;
       font-weight: 700;
       padding: 3px 7px;
       border-radius: var(--radius-pill);
     }

     /* Benefits list */
     .benefits {
       display: flex;
       align-items: flex-start;
       justify-content: space-between;
       gap: var(--space-2);
     }

     .benefits__list {
       list-style: none;
       margin: 0;
       padding: 0;
       display: flex;
       flex-direction: column;
       gap: var(--space-3);
       flex: 1;
     }

     .heading_benefits__list{
       display: flex;
       justify-content: space-between;
       align-items: center;
     }

     .benefits__item {
       display: flex;
       align-items: flex-start;
       gap: var(--space-2);
       font-size: var(--fs-sm);
       line-height: 1.4;
     }

     .benefits__icon {
       flex-shrink: 0;
       width: 18px;
       height: 18px;
       border-radius: 50%;
       background: var(--color-success);
       color: var(--color-white);
       display: flex;
       align-items: center;
       justify-content: center;
       font-size: 0.65rem;
       margin-top: 1px;
     }

     .benefits__link {
       font-size: var(--fs-xs);
       text-decoration: underline;
       color: var(--color-text);
       white-space: nowrap;
       background: none;
       border: none;
       cursor: pointer;
       font-family: inherit;
       padding: 0;
     }

     /* Free gift */
     .gift {
       display: flex;
       align-items: center;
       gap: var(--space-3);
       background: var(--color-bg-muted);
       border-radius: var(--radius-md);
       padding: var(--space-3);
     }

     .gift__image {
       width: 48px;
       height: 48px;
       border-radius: var(--radius-sm);
       background: #2b2320;
       flex-shrink: 0;
       overflow: hidden;
     }

     .gift__image img {
       width: 100%;
       height: 100%;
       object-fit: cover;
       display: block;
     }

     .gift__info {
       flex: 1;
       display: flex;
       flex-direction: column;
       gap: 2px;
     }

     .gift__title {
       font-size: var(--fs-sm);
       font-weight: 600;
     }

     .gift__badge {
       align-self: flex-start;
       background: var(--color-accent-bg);
       color: var(--color-white);
       font-size: var(--fs-xs);
       font-weight: 600;
       padding: var(--space-1) var(--space-2);
       border-radius: 7px;
     }

     .gift__price {
       color: var(--color-accent);
       font-weight: 600;
       font-size: var(--fs-sm);
     }

     /* Delivery interval select */
     .field {
       display: flex;
       flex-direction: column;
       gap: var(--space-1);
     }

     .field__label {
       font-size: var(--fs-sm);
       font-weight: 600;
     }

     .select-wrap {
       position: relative;
     }

     .select {
       width: 100%;
       appearance: none;
       -webkit-appearance: none;
       background: var(--color-bg);
       border: 1px solid var(--color-border);
       border-radius: var(--radius-md);
       padding: var(--space-3) var(--space-6) var(--space-3) var(--space-3);
       font-size: var(--fs-md);
       font-family: inherit;
       color: var(--color-text);
       cursor: pointer;
     }


     .select-wrap::after {
       content: "";
       position: absolute;
       right: var(--space-3);
       top: 50%;
       width: 5px;
       height: 5px;
       border-right: 2px solid var(--color-text);
       border-bottom: 2px solid var(--color-text);
       transform: translateY(-70%) rotate(45deg);
       pointer-events: none;
     }

     .info-row {
       display: flex;
       align-items: center;
       gap: var(--space-2);
       font-size: var(--fs-xs);
       color: var(--color-text-muted);
     }

     .select-wrap-info-row {
       display: flex;
       justify-content: space-between;
       align-items: center;
     }

     .info-row__icon {
       width: 16px;
       height: 16px;
       border: 1.5px solid var(--color-text-muted);
       border-radius: 50%;
       flex-shrink: 0;
       display: flex;
       align-items: center;
       justify-content: center;
       font-size: 0.6rem;
     }

     .info-row__link {
       margin-left: auto;
       text-decoration: underline;
       color: var(--color-text);
       background: none;
       border: none;
       font-family: inherit;
       font-size: var(--fs-xs);
       cursor: pointer;
     }

     .stock-status {
       display: flex;
       align-items: center;
       gap: var(--space-2);
       font-size: var(--fs-xs);
       color: var(--color-text-muted);
     }

     .stock-status__dot {
       width: 8px;
       height: 8px;
       border-radius: 50%;
       background: var(--color-success);
       flex-shrink: 0;
     }

     /* CTA */
     .cta {
       width: 100%;
       padding: var(--space-4);
       background: var(--color-cta);
       color: var(--color-white);
       border: none;
       border-radius: var(--radius-md);
       font-size: var(--fs-md);
       font-weight: 600;
       cursor: pointer;
       font-family: inherit;
       transition: background var(--transition-fast);
     }

     .cta:hover {
       background: var(--color-cta-hover);
     }

     .cta:focus-visible {
       outline: 2px solid var(--color-border-active);
       outline-offset: 2px;
     }

     /* Bottom info list */
     .meta-list {
       list-style: none;
       margin: 0;
       padding: 0;
       display: flex;
       flex-direction: column;
       gap: var(--space-2);
     }

     .meta-list__item {
       display: flex;
       align-items: center;
       gap: var(--space-2);
       font-size: var(--fs-sm);
       color: var(--color-text-muted);
     }

     .meta-list__icon {
       width: 18px;
       height: 18px;
       flex-shrink: 0;
       display: flex;
       align-items: center;
       justify-content: center;
     }
  </style>
{% endif %}
<style>
  .purchase{
    display: none;
  }
</style>
<section
  id="MainProduct-{{ section.id }}"
  class="section-{{ section.id }}-padding section-{{ section.id }}-margin"
  data-section="{{ section.id }}"
>
  {{ 'section-main-product.css' | asset_url | stylesheet_tag }}
  {{ 'component-accordion.css' | asset_url | stylesheet_tag }}
  {{ 'component-price.css' | asset_url | stylesheet_tag }}
  {{ 'component-slider.css' | asset_url | stylesheet_tag }}
  {{ 'component-rating.css' | asset_url | stylesheet_tag }}
  {{ 'component-deferred-media.css' | asset_url | stylesheet_tag }}
  {%- if product.quantity_price_breaks_configured? -%}
    {{ 'component-volume-pricing.css' | asset_url | stylesheet_tag }}
  {%- endif -%}

  {%- style -%}
      @media screen and (min-width: 750px) {
        .product--thumbnail_slider .verticle_thumb.verticle_left .thumbnail-slider{
          margin-top:0;
          padding-left:0;
        }
        .section-template--30169128337745__main-padding {
        padding-top: 10px !important;
        }

        .full_w{
          max-width:100% !important;
        }

        .product--small:not(.product--no-media) .product__media-wrapper{
                  max-width: 55%;
            width: calc(55% - var(--grid-desktop-horizontal-spacing) / 2);
        }
        .product__info-wrapper.grid__item.product__info-wrapper--extra-padding {
            max-width: 45%;
            width: calc(45% - var(--grid-desktop-horizontal-spacing) / 2);
        }
        .custom-class-tsf.thumbnail[aria-current="true"], .custom-class-main-tsf.thumbnail[aria-current="true"], .custom-class-menu button[aria-current="true"],body .custom-class-tsf,body .custom-class-tsf.thumbnail[aria-current="true"] {
          border:none !important;
           border-bottom:none !important;
           border-radius:10px !important;
        }
        .product-media-container .media {
           border-radius:10px !important;
        }
        .product__media.media.media--transparent{

        }  border-radius:10px !important;

       .product--thumbnail .product__media-list,
      .product--thumbnail_slider .product__media-list {
        padding-bottom: 0;
      }

      .product__media-list {
        padding-right: 0;
      }

      .product--thumbnail .product__media-item:not(.is-active),
      .product--thumbnail_slider .product__media-item:not(.is-active) {
        display: unset;
      }

    /*   .product-media-modal__content > .product__media-item--variant.product__media-item--variant {
        display: unset;
      }

      .product-media-modal__content > .product__media-item--variant:first-child {
        display: unset;
      }  */
      .product__media-list .product__media-item {
          width: 100%;
      }

      /* Product Page verticle Thumbnail start */

        .product--thumbnail .verticle_thumb,
        .product--thumbnail_slider .verticle_thumb {
          display: -webkit-box;
          display: -moz-box;
          display: -ms-flexbox;
          display: -webkit-flex;
          display: flex;
          justify-content: space-between;
        }

        .product--thumbnail .verticle_thumb .slider-mobile-gutter,
        .product--thumbnail_slider .verticle_thumb .slider-mobile-gutter {
          flex: 0 0 84%;
          width: 84%;
        }

        .verticle_thumb .thumbnail-slider.slider-mobile-gutter {
          width: 18%;
          display: inline-block;
          flex: 0 0 18%;
        }
        .verticle_thumb.verticle_left .thumbnail-slider .thumbnail-list.slider--tablet-up {
          padding: 0 4.5rem 0 0rem;
          grid-gap: 2rem;
        }
        .verticle_thumb.verticle_right .thumbnail-slider .thumbnail-list.slider--tablet-up {
          padding: 1.5rem 4.5rem 1.5rem 0;
          grid-gap: 2rem;
        }
        .product--thumbnail_slider .verticle_thumb.verticle_left .thumbnail-slider .slider-button--prev,
        .product--thumbnail_slider .verticle_thumb.verticle_right .thumbnail-slider .slider-button--prev{
          left: -20px;
        }
        .product--thumbnail_slider .verticle_thumb.verticle_left .thumbnail-slider .slider-button--next,
        .product--thumbnail_slider .verticle_thumb.verticle_right .thumbnail-slider .slider-button--next{
          right: -15px;
        }
        .verticle_thumb.verticle_left .slider-mobile-gutter { order: 2;}
        .verticle_thumb.verticle_left .thumbnail-slider.slider-mobile-gutter { order: 1;}
        .verticle_thumb .thumbnail-list  { display: block; }
        .verticle_thumb .thumbnail-list .thumbnail-list__item { margin-bottom: 0;}
        .product--thumbnail_slider .verticle_thumb.verticle_left { justify-content: end;}
        .product--thumbnail_slider .verticle_thumb.verticle_left .thumbnail-slider { position: absolute; left: 0; top: 0; height: auto; width: 77%;}
        .product--thumbnail_slider .verticle_thumb .thumbnail-slider { position: relative; transform: rotate(90deg) translate(0px, -100%); transform-origin: left top; flex: 0 0 16%; height: 100%;}
        .product--thumbnail_slider .verticle_thumb .thumbnail-slider .thumbnail-list .thumbnail-list__item { transform: rotate(-90deg);  width: calc(20% - 0.8rem);}
        .product--thumbnail_slider .verticle_thumb.verticle_right .thumbnail-slider { flex: 0 0 80%; height: auto; height: 100%; margin-left: 1%;}

        /* Product Page verticle Thumbnail End */

    /*   .full_pro_width.verticle_left .slider-mobile-gutter,.full_pro_width.verticle_right .slider-mobile-gutter { flex: 0 0 100%; width: 100%; } */

        .verticle_thumb.verticle_left .thumbnail-list:not(.slider--tablet-up) .thumbnail-list__item {
            margin: 0 1.5rem 1.5rem 0;
        }
        .verticle_thumb.verticle_right .thumbnail-list:not(.slider--tablet-up) .thumbnail-list__item {
            margin: 0 0 1.5rem 1.5rem;
        }
    }

    @media screen and (max-width: 989px) and (min-width: 750px) {
      .product--thumbnail_slider .verticle_thumb.verticle_left .thumbnail-slider {
          width: 88%;
      }
      .verticle_thumb.verticle_left .thumbnail-slider .thumbnail-list.slider--tablet-up,
      .verticle_thumb.verticle_right .thumbnail-slider .thumbnail-list.slider--tablet-up{
          grid-gap: 1rem;
      }
      .verticle_thumb.verticle_left .thumbnail-list:not(.slider--tablet-up) .thumbnail-list__item {
          margin: 0 1rem 1rem 0;
      }
      .verticle_thumb.verticle_right .thumbnail-list:not(.slider--tablet-up) .thumbnail-list__item {
          margin: 0 0 1rem 1rem;
      }
    }

    .product__media-wrapper .product__media-gallery slider-component .slider-buttons,
    .product--thumbnail_slider .slider-mobile-gutter .slider-button{
        opacity: 0;
        transition: var(--duration-long);
    }
    .product__media-wrapper .product__media-gallery slider-component:hover .slider-buttons,
    .product--thumbnail_slider .slider-mobile-gutter:hover .slider-button{
        opacity: 1;
    }


        {% if section.settings.gallery_layout == 'thumbnail_slider_verticle'  or section.settings.gallery_layout == 'thumbnail_right_slider_verticle' %}
          .swiper-container{
            max-height: 750px;
          }
          .thumbnail-slider{
            max-width: 22%;
            width: 100%;
            float: left;
            display: block;
            padding: 16px 15px;
          }
          .main_product_img{
            max-width: 78%;
            width: 100%;
            float: left;
            padding: 0px 15px;
          }
          .product__media-wrapper media-gallery{
            display: inline-flex;
            width: 100%;
            flex-direction: row-reverse;
          }
          .product__media-wrapper .swiper-vertical .swiper-button-prev{
            top: 0;
            left: 50% !important;
            rotate: 90deg;
            translate: -50%;
            transform: translateY(0) !important;
          }
          .product__media-wrapper .swiper-vertical .swiper-button-next{
            bottom: 0;
            top: auto;
            left: 50%;
            rotate: 90deg;
            translate: -50%;
            transform: translateY(0) !important;
          }
           @media screen and (min-width: 992px){
            .product__media-item:not(.is-active), .product__media-item:not(.is-active) {
              display: none;
            }
          }
          @media screen and (max-width: 1366px){
            .swiper-container {
              max-height: 480px;
            }
          }
          @media screen and (max-width: 1199px){
            .thumbnail-slider{
              max-width: 25%;
            }
            .main_product_img{
              max-width: 75%;
            }
          }
          @media screen and (max-width: 991px){
            .product--thumbnail_slider_verticle .product__media-item, .product--thumbnail_right_slider_verticle .product__media-item{
               width: 100%;
             }
            .thumbnail-slider{
              display: none;
            }
            .main_product_img{
              max-width: 100%;
            }
          }
          {% if section.settings.gallery_layout == 'thumbnail_right_slider_verticle' %}
            .product__media-wrapper media-gallery{
              flex-direction: revert;
            }
          {% endif %}
        {% endif %}
        .product--thumbnail_slider .swiper-container{
          height: auto;
        }
        .product--thumbnail_slider .thumbnail-list, .product--thumbnail_slider_verticle .thumbnail-list, .product--thumbnail_right_slider_verticle .thumbnail-list, .product--horizontal_thumbnail .thumbnail-list {
          display: flex;
          flex-wrap: unset;
          grid-gap: 0;
        }
        .product--thumbnail_slider .swiper-slide{
          width: 21.50%;
        }
        @media screen and (min-width: 750px){
          .product--product_sticky .thumbnail-slider, .product--stacked .thumbnail-slider, .product--columns .thumbnail-slider{
            display: none;
          }
          .slider-button{
            display: none;
          }
        }
        .sticky_cart{
          border-top: 1px solid {{ section.settings.border_color }};
          background-color: {{ section.settings.sticky_bg }};
        }
        .sticky_cart .sticky-page .h3{
          color: {{ settings.card_product_title_color }};
        }
        .sticky_cart .price-item.price-item--regular{
          color: {{ settings.card_product_sale_price_color }};
        }
        .sticky_cart .price-item.price-item--sale{
           color: {{ settings.card_product_regular_price_color }};
        }
        .sticky_cart .quantity:after{
          border: 1px solid {{ settings.q_button_border }};
        }
        .sticky_cart .quantity__input{
          color: {{ settings.q_button_color }};
        }
        .sticky_cart .quantity__button{
          background-color: {{ settings.q_button_bg }} !important;
          color: {{ settings.q_button_color }};
        }
        .sticky_cart .quantity__button:hover{
          background-color: {{ settings.q_button_hr_bg }} !important;
          color: {{ settings.q_button_hr_color }};
        }
        .sticky_cart select, .sticky_cart .quick-add__submit{
           background-color: {{ settings.button_bg }};
          color: {{ settings.button_text }};
        }
        .sticky_cart select:hover, .sticky_cart .quick-add__submit:hover{
           background-color: {{ settings.button_hr_bg }};
          color: {{ settings.button_hr_text }};
        }
        .sticky_cart .sticky_cart_close{
          color: {{ settings.page_title_text_color }};
        }
        .sticky_cart .sticky_cart_close:hover{
          color: {{ settings.theme_text_hover_color }}
        }
        .complementary-products .accordion summary {
          background:{{ settings.input_bg_color }};
          color: {{ settings.input_text_color }};
        }
        .product__info-wrapper .accordion details {
          border: 1px solid {{ settings.border_color }};
          border-radius: var(--buttons-radius-outset);
        }
        .product__info-wrapper .countdown-timer .main-c-motel{
          background-color: {{ settings.input_bg_color }};
          color:{{ settings.input_text_color }};
        }
        {% for block in section.blocks %}
          {% if block.type == 'text' %}
            .product__info-wrapper .product__text{
              color: {{ block.settings.text_color }};
            }
          {% endif %}
        {% endfor %}
        {% for block in section.blocks %}
          {% if block.type == 'title' %}
            .product__info-wrapper .product__title h1,
            .product__info-wrapper .product__title .h1{
              color: {{ block.settings.product_title_color }};
            }
          {% endif %}
          {% if block.type == 'inventory' %}
            .product__info-wrapper .product__inventory{
              color: {{ block.settings.inventory_text }};
            }
          {% endif %}
        {% endfor %}
        {% for block in section.blocks %}
          {% if block.type == 'price' %}
            .product__info-wrapper .price-item.price-item--regular{
              color: {{ block.settings.regular_price }};
            }
            .product__info-wrapper .price-item.price-item--sale{
              color: {{ block.settings.sale_price }};
            }
          {% endif %}
        {% endfor %}
        {% for block in section.blocks %}
          {% if block.type == 'sku' %}
            .product__info-wrapper .product__sku{
              color: {{ block.settings.sku_heading }};
            }
          {% endif %}
        {% endfor %}
        {% for block in section.blocks %}
          {% if block.type == 'comparewishlist' %}
          .product__info-wrapper .product-hover button{
            background-color: {{ settings.card_button_bg }};
          }
          .product__info-wrapper .product-hover button svg path{
            stroke: {{ settings.card_button_text }};
          }
          .product__info-wrapper .quick-add__submit:hover,
          .product__info-wrapper .product-hover button:hover{
            background-color: {{ settings.card_button_hr_bg }};
            color: {{ settings.card_button_hr_text }};
          }
          .product__info-wrapper .quick-add__submit:hover svg path,
          .product__info-wrapper .product-hover button:hover svg path{
            stroke: {{ settings.card_button_hr_text }};
          }
          .product__info-wrapper .product-hover .active .icon.icon-wishlist,
          .product__info-wrapper .product-hover .active .icon.icon-compare{
            fill: {{ settings.card_button_text }};
            color: {{ settings.card_button_text }};
          }
        .product__info-wrapper .product-hover .active .icon.icon-wishlist path,
          .product__info-wrapper .product-hover .active .icon.icon-compare path{
            stroke: {{ settings.card_button_text }};
          }
          .product__info-wrapper .product-hover button:hover.active .icon.icon-wishlist,
          .product__info-wrapper .product-hover button:hover.active .icon.icon-compare{
            fill: {{ settings.card_button_hr_text }};
            color: {{ settings.card_button_hr_text }};
          }
          .product__info-wrapper .product-hover button:hover.active .icon.icon-wishlist path,
          .product__info-wrapper .product-hover button:hover.active .icon.icon-compare path{
            stroke: {{ settings.card_button_hr_text }};
          }
          {% endif %}
        {% endfor %}
        {% for block in section.blocks %}
          {% if block.type == 'variant_picker' %}
            .varians_heading{
               background-color: {{ block.settings.variant_bg }};
             }
            .product__info-wrapper fieldset.product-form__input .form__label, .varians_heading{
              color: {{ block.settings.var_heading }};
            }
            .product__info-wrapper .product-form__input input[type=radio]:checked+label{
              border: 1px solid {{ block.settings.active_variant_color }};
              background-color: {{ block.settings.active_variant_bg }};
              color: {{ block.settings.active_variant_color }};
            }
            .product__info-wrapper .product-form__input input[type=radio]+label{
              border: 1px solid {{ block.settings.variant_color }};
              background-color: {{ block.settings.variant_bg }};
              color: {{ block.settings.variant_color }};
            }
           {% endif %}
        {% endfor %}

        {% for block in section.blocks %}
          {% if block.type == 'quantity_selector' %}
            .product__info-wrapper .product-form__quantity .form__label{
              color: {{ block.settings.quantity_heading }};
            }
          {% endif %}
        {% endfor %}
        .product__info-wrapper .quantity:after{
          border: 1px solid {{ settings.q_button_border }};
        }
        .product__info-wrapper .quantity__input{
          color: {{ settings.q_button_color }};
        }
        .product__info-wrapper .product-form__quantity .quantity__button{
          background-color: {{ settings.q_button_bg }} !important;
          color: {{ settings.q_button_color }};
        }
        .product__info-wrapper .product-form__quantity .quantity__button:hover{
          background-color: {{ settings.q_button_hr_bg }} !important;
          color: {{ settings.q_button_hr_color }};
        }
        {% for block in section.blocks %}
          {% if block.type == 'buy_buttons' %}
            .product__info-wrapper .product-form__submit{
      background-color: {{ block.settings.add_btn_bg }};
      color: {{ block.settings.add_btn_color }};
      font-size: 16px !important;
    }
    @media screen and (min-width: 750px) {
      .product__info-wrapper .product-form__submit {
        font-size: 21px !important;
      }
    }
            .product__info-wrapper .product-form__submit:hover{
              background-color: {{ block.settings.add_btn_hr_bg }};
              color: {{ block.settings.add_btn_hr_color }};
            }
            .product__info-wrapper .shopify-payment-button__button{
              background-color: {{ block.settings.add_btn_hr_bg }};
              color: {{ block.settings.add_btn_hr_color }};
            }
            .product__info-wrapper .shopify-payment-button__button:hover{
              background-color: {{ block.settings.add_btn_bg }};
              color: {{ block.settings.add_btn_color }};
            }
            .product__info-wrapper .product_category_feature .form__label{
              color: {{ block.settings.collection_label_color }};
            }
           .product__info-wrapper .product_category_feature .main_collection{
              color: {{ block.settings.collection_color }};
              border-bottom: 1px solid {{ block.settings.collection_color }};
            }
            .product__info-wrapper .product_category_feature .main_collection:hover{
              color: {{ block.settings.collection_hr_color }};
              border-bottom: 1px solid {{ block.settings.collection_hr_color }};
            }
          {% endif %}
        {% endfor %}
        {% for block in section.blocks %}
          {% if block.type == 'popup' %}
            .product-popup-modal__opener .product-popup-modal__button{
              color: {{ block.settings.sizechart_color }};
            }
          {% endif %}
        {% endfor %}
        {% for block in section.blocks %}
          {% if block.type == 'icon-with-text' %}
            .icon-with-text__item svg, .icon-with-text__item span{
              fill: {{ block.settings.icon_block_text }};
              color: {{ block.settings.icon_block_text }};
            }
          {% endif %}
        {% endfor %}
        {% for block in section.blocks %}
          {% if block.type == 'collapsible_tab' %}
            .product__accordion .icon-caret, .product__accordion .icon-accordion, .product__accordion .accordion__title{
              fill: {{ block.settings.acco_title }} !important;
              color: {{ block.settings.acco_title }};
            }
            .product__accordion .accordion__content{
              color: {{ block.settings.acco_content }};
            }
          {% endif %}
        {% endfor %}
        {% for block in section.blocks %}
          {% if block.type == 'description' %}
            .product__description{
              color: {{ block.settings.description_color }};
            }
          {% endif %}
        {% endfor %}
        {% for block in section.blocks %}
          {% if block.type == 'view' %}
            .product__info-wrapper .visitors_user{
              background-color: {{ block.settings.visitor_bg }};
              color:{{ block.settings.visitor_text }};
              fill:{{ block.settings.visitor_text }};
            }
          {% endif %}
          {% if block.type == 'product_sales' %}
            .social-proof-sales{
              background-color: {{ block.settings.sale_bg }};
              color:{{ block.settings.sale_text }};
              fill:{{ block.settings.sale_text }};
            }
          {% endif %}
        {% endfor %}
        .badge{
          background-color: {{ settings.badge_bg_color }};
          color: {{ settings.badge_text_color }};
        }
        .product__info-wrapper .product__tax,
        .pickup-availability-preview .pickup-availability-button,
        .pickup-availability-header .pickup-availability-drawer-title,
        .pickup-availability-list__item .h4,
        .pickup-availability-preview .pickup-availability-button:hover,
        .recipient-checkbox>svg,
        .recipient-checkbox span,
        .recipient-form-field-label span{
          color: {{ section.settings.text_heading_color }};
        }
        .pickup-availability-preview .caption-large,
        .pickup-availability-preview .caption-large span,
        .pickup-availability-preview .caption,
        .pickup-availability-variant,
        .pickup-availability-preview svg path,
        .pickup-availability-preview,
        .pickup-availability-address{
          color: {{ section.settings.text_description_color }};
          fill: {{ section.settings.text_description_color }};
        }
        pickup-availability-drawer[open]{
          background-color: {{ section.settings.drawer_bg }};
        }
        .pickup-availability-drawer-button,
        .share-button__button,
        .share-button__fallback button,
        .product-popup-modal__toggle{
          background-color: {{ settings.button_bg }};
          color: {{ settings.button_text }};
        }
        .pickup-availability-drawer-button:hover,
        .share-button__button:hover,
        .share-button__fallback button:hover,
        .product-popup-modal__toggle:hover{
          background-color: {{ settings.button_hr_bg }};
          color: {{ settings.button_hr_text }};
        }
        .product__media-wrapper .slider-mobile-gutter .slider-button.slider-button--next,
        .product__media-wrapper .slider-mobile-gutter .slider-button.slider-button--prev,
        .product__media-wrapper .swiper-button-next,
        .product__media-wrapper .swiper-button-prev{
          border: 1px dashed {{ settings.button_bg }};
        }
        .product__media-wrapper .slider-mobile-gutter .slider-button.slider-button--next svg path,
        .product__media-wrapper .slider-mobile-gutter .slider-button.slider-button--prev svg path{
          color: {{ settings.button_bg }};
          stroke: {{ settings.button_bg }};
        }
        .product__media-wrapper .slider-mobile-gutter .slider-button.slider-button--next:hover,
        .product__media-wrapper .slider-mobile-gutter .slider-button.slider-button--prev:hover,
        .product__media-wrapper .swiper-button-next:hover,
        .product__media-wrapper .swiper-button-prev:hover{
          border: 1px dashed {{ settings.button_hr_bg }};
        }
        .product__media-wrapper .slider-mobile-gutter .slider-button.slider-button--next:hover svg path,
        .product__media-wrapper .slider-mobile-gutter .slider-button.slider-button--prev:hover svg path{
          color: {{ settings.button_hr_bg }};
          stroke: {{ settings.button_hr_bg }};
        }
        .product__info-wrapper variant-radios,
        .icon-with-text--horizontal .icon-with-text__item{
          border: 1px solid {{ section.settings.border_color }};
        }
        .section-{{ section.id }}-padding {
          padding-top: {{ section.settings.padding-top }}px;
          padding-bottom: {{ section.settings.padding-bottom }}px;
        }
        @media screen and (max-width: 1429px) {
          .section-{{ section.id }}-padding {
            padding-top: {{ section.settings.padding-top-1429 }}px;
            padding-bottom: {{ section.settings.padding-bottom-1429 }}px;
          }
        }
        @media screen and (max-width: 1199px) {
          .section-{{ section.id }}-padding {
            padding-top: {{ section.settings.padding-top-1199 }}px;
            padding-bottom: {{ section.settings.padding-bottom-1199 }}px;
          }
        }
        @media screen and (max-width: 991px) {
          .section-{{ section.id }}-padding {
            padding-top: {{ section.settings.padding-top-991 }}px;
            padding-bottom: {{ section.settings.padding-bottom-991 }}px;
          }
        }
            body .custom-class-tsf.thumbnail{
                 box-shadow:none !important;
            }
        @media screen and (max-width: 767px) {
          .grid--1-col .grid__item.images_w{
                overflow: hidden;
        border-radius: 0;
            width: calc(100% + 30px);
        margin-left: -15px;
        max-width: inherit;
          }
          .global-media-settings{
            overflow:hidden !important;
                    border-radius: 0;
          }
          .product--thumbnail_slider .thumbnail-slider{
            margin-top:0 !important;
            padding:0 10px !important;
          }
          .thumbnail-list__item.slider__slide{
            width:calc(20% - 8px);
                    padding-bottom: 10px !important;

          }
          .product-media-container{
            max-width:100%;
          }
          html body .custom-class-tsf.thumbnail{
            padding-bottom:10px !important;
          }
         body .thumbnail-slider .thumbnail-list.slider{
          gap:8px !important;
          margin:0;
          padding:0;
          }
         body .custom-class-tsf.thumbnail{
                 box-shadow:none !important;
          }
              .section-template--30169128337745__main-padding {
            padding-top: 0px !important;
              }
          .media>img, .media>video,.product-media-container .media {
            border-radius:0 !important;
          }
          .slider.slider--everywhere .slider__slide{
            width:100% !important;
          }
          .section-{{ section.id }}-padding {
            padding-top: {{ section.settings.padding-top-767 }}px;
            padding-bottom: {{ section.settings.padding-bottom-767 }}px;
          }
        }
        @media screen and (max-width: 575px) {
          .section-{{ section.id }}-padding {
            padding-top: {{ section.settings.padding-top-575 }}px;
            padding-bottom: {{ section.settings.padding-bottom-575 }}px;
          }
        }
  {%- endstyle -%}

  <script src="{{ 'product-info.js' | asset_url }}" defer="defer"></script>
  <script src="{{ 'product-form.js' | asset_url }}" defer="defer"></script>
  {%- if product.quantity_price_breaks_configured? -%}
    <script src="{{ 'show-more.js' | asset_url }}" defer="defer"></script>
    <script src="{{ 'price-per-item.js' | asset_url }}" defer="defer"></script>
  {%- endif -%}

  {% if section.settings.image_zoom == 'hover' %}
    <script id="EnableZoomOnHover-main" src="{{ 'magnify.js' | asset_url }}" defer="defer"></script>
  {% endif %}
  {%- if request.design_mode -%}
    <script src="{{ 'theme-editor.js' | asset_url }}" defer="defer"></script>
  {%- endif -%}

  {%- assign first_3d_model = product.media | where: 'media_type', 'model' | first -%}
  {%- if first_3d_model -%}
    {{ 'component-product-model.css' | asset_url | stylesheet_tag }}
    <link
      id="ModelViewerStyle"
      rel="stylesheet"
      href="https://cdn.shopify.com/shopifycloud/model-viewer-ui/assets/v1.0/model-viewer-ui.css"
      media="print"
      onload="this.media='all'"
    >
    <link
      id="ModelViewerOverride"
      rel="stylesheet"
      href="{{ 'component-model-viewer-ui.css' | asset_url }}"
      media="print"
      onload="this.media='all'"
    >
  {%- endif -%}

  {% assign variant_images = product.images | where: 'attached_to_variant?', true | map: 'src' %}
  <div class="page-width full_w">
    <div class="product product--{{ section.settings.media_size }} product--{{ section.settings.media_position }} product--{{ section.settings.gallery_layout }} product--mobile-{{ section.settings.mobile_thumbnails }} grid grid--1-col {% if product.media.size > 0 %}grid--2-col-tablet{% else %}product--no-media{% endif %}">
      <div class="grid__item product__media-wrapper images_w" id="gall-{{ product.id }}">
        {% render 'product-media-gallery', variant_images: variant_images %}
      </div>
      <div class="product__info-wrapper grid__item{% if settings.page_width > 1400 and section.settings.media_size == "small" %} product__info-wrapper--extra-padding{% endif %}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
        <product-info
          id="ProductInfo-{{ section.id }}"
          data-section="{{ section.id }}"
          data-url="{{ product.url }}"
          class="product__info-container{% if section.settings.enable_sticky_info %} product__column-sticky{% endif %}"
        >
          {%- assign product_form_id = 'product-form-' | append: section.id -%}

          {%- for block in section.blocks -%}
            {%- case block.type -%}
              {%- when '@app' -%}
                {% render block %}
              {%- when 'text' -%}
                <p
                  class="product__text inline-richtext"
                  {{ block.shopify_attributes }}
                >
                  {{- block.settings.text -}}
                </p>
              {%- when 'title' -%}
                <div class="product__title" {{ block.shopify_attributes }}>
                  <h1 class="h2">{{ product.title | escape }}</h1>
                  <a href="{{ product.url }}" class="product__title">
                    <div class="h1">
                      {{ product.title | escape }}
                    </div>
                  </a>
                </div>
                {% assign product_metafields = product.metafields %}
                {% assign countdown_date = product_metafields.card_product.countdown_date %}

                {% if countdown_date %}
                  {% assign countdown_time = countdown_date | date: '%s' %}
                  {% assign current_time = 'now' | date: '%s' %}
                  {% assign time_diff = countdown_time | minus: current_time %}
                  {% assign days_left = time_diff | divided_by: 86400 %}
                  {% assign hours_left = time_diff | divided_by: 3600 | modulo: 24 %}
                  {% assign minutes_left = time_diff | divided_by: 60 | modulo: 60 %}
                  {% assign seconds_left = time_diff | modulo: 60 %}

                  <div
                    class="countdown-timer countdown__wrapper clockdiv"
                    id="ui-countdown"
                    data-date="{{ countdown_date  | date: '%B %d, %Y %H:%M:%S' }}"
                  >
                    <div class="main-c-motel">
                      <div class="countdown-item days countdown__value" id="ui-countdown-days">{{ days_left }}</div>
                      <div class="bigtext_countdown">{{ 'products.product.quantity.day' | t }}</div>
                    </div>
                    <div class="main-c-motel">
                      <div class="countdown-item hours countdown__value" id="ui-countdown-hours">{{ hours_left }}</div>
                      <div class="bigtext_countdown">{{ 'products.product.quantity.hours' | t }}</div>
                    </div>
                    <div class="main-c-motel">
                      <div class="countdown-item minutes countdown__value" id="ui-countdown-minutes">
                        {{ minutes_left }}
                      </div>
                      <div class="bigtext_countdown">{{ 'products.product.quantity.min' | t }}</div>
                    </div>
                    <div class="main-c-motel">
                      <div class="countdown-item seconds countdown__value" id="ui-countdown-seconds">
                        {{ seconds_left }}
                      </div>
                      <div class="bigtext_countdown">{{ 'products.product.quantity.sec' | t }}</div>
                    </div>
                  </div>
                {% endif %}
              {%- when 'view' -%}
                <div class="visitors_user">
                  <div class="eye_user">{% render 'icon-eye' %}</div>
                  <div id="number" style="display: block;"></div>
                  <span>{{ block.settings.count_text }}</span>
                </div>
                <script>
                  document.addEventListener('DOMContentLoaded', function() {
                    // Function to generate a random integer between min and max
                    function randomInt(min, max) {
                      return Math.floor(Math.random() * (max - min)  + min);
                    }

                    // Function to update the numberDiv with a random number
                    function updateNumberDiv(min, max) {
                      var numberDiv = document.getElementById('number');
                      numberDiv.textContent = randomInt(min, max);
                    }

                    // Function to call updateNumberDiv at regular intervals
                    function getNumberBetweenEvery(min, max, secs) {
                      updateNumberDiv({{ block.settings.count_range_min }}, {{ block.settings.count_range_max }}); // Initial call to display a random number

                      setInterval(function() {
                        updateNumberDiv({{ block.settings.count_range_min }}, {{ block.settings.count_range_max }});
                      }, secs * {{ block.settings.view_duration }}); // Convert secs to milliseconds
                    }

                    // Call getNumberBetweenEvery with your desired parameters
                    getNumberBetweenEvery(100, 1000, 1);
                  });
                </script>
              {%- when 'price' -%}
                <div class="no-js-hidden" id="price-{{ section.id }}" role="status" {{ block.shopify_attributes }}>
                  {%- render 'price',
                    product: product,
                    use_variant: true,
                    show_badges: true,
                    price_class: 'price--large'
                  -%}
                </div>
                {%- if product.quantity_price_breaks_configured? -%}
                  <div class="volume-pricing-note" id="Volume-Note-{{ section.id }}">
                    <span>{{ 'products.product.volume_pricing.note' | t }}</span>
                  </div>
                {%- endif -%}
                <div class="product__tax caption rte">
                  {%- if cart.taxes_included -%}
                    {{ 'products.product.include_taxes' | t }}
                  {%- endif -%}
                  {%- if shop.shipping_policy.body != blank -%}
                    {{ 'products.product.shipping_policy_html' | t: link: shop.shipping_policy.url }}
                  {%- endif -%}
                </div>
                <div {{ block.shopify_attributes }}>
                  {%- assign product_form_installment_id = 'product-form-installment-' | append: section.id -%}
                  {%- form 'product', product, id: product_form_installment_id, class: 'installment caption-large' -%}
                    <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
                    {{ form | payment_terms }}
                  {%- endform -%}
                </div>
              {%- when 'product_sales' -%}
                <div class="social-proof-sales">
                  <div class="flash_total_qty"></div>
                  <div class="flash_in_hour">Hours</div>
                </div>
                <script>
                document.addEventListener('DOMContentLoaded', function() {
                  function flashSoldBar() {
                    var minQty = parseInt("{{ block.settings.minqty }}");
                    var maxQty = parseInt("{{ block.settings.maxqty }}");
                    var minTime = parseInt("{{ block.settings.min_value }}");
                    var maxTime = parseInt("{{ block.settings.max_value }}");
              
                    minQty = Math.ceil(minQty);
                    maxQty = Math.floor(maxQty);
                    minTime = Math.ceil(minTime);
                    maxTime = Math.floor(maxTime);
              
                    var qty = Math.floor(Math.random() * (maxQty - minQty + 1)) + minQty;
                    qty = Math.max(qty, minQty);
                    qty = Math.min(qty, maxQty);
              
                    var time = Math.floor(Math.random() * (maxTime - minTime + 1)) + minTime;
                    time = Math.max(time, minTime);
                    time = Math.min(time, maxTime);
              
                    var flashTotalQty = document.querySelector('.flash_total_qty');
                    var flashInHour = document.querySelector('.flash_in_hour');
              
                    if (flashTotalQty && flashInHour) {
                      flashTotalQty.innerHTML = `<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="feather feather-x-circle"><circle cx="12" cy="12" r="10"/><line x1="15" y1="9" x2="9" y2="15"/><line x1="9" y1="9" x2="15" y2="15"/></svg> <span>{{ block.settings.item_sold }}</span>${qty}<span>{{ block.settings.products }}</span>`;
                      flashInHour.innerHTML = `${time} {{ block.settings.hours }}`;
                    }
                  }
              
                  flashSoldBar(); // Initial call
              
                  var flashInterval = setInterval(flashSoldBar, {{ block.settings.product-sale-timer }}); // Call every 5 seconds
                });
                </script>
              {%- when 'inventory' -%}
                {%- assign quantity = product.variants.first.inventory_quantity -%}
                {% if quantity != 0 %}
                  <div class="main_inventory_wrapper">
                    <p
                      class="product__inventory no-js-hidden{% if block.settings.text_style == 'uppercase' %} caption-with-letter-spacing{% elsif block.settings.text_style == 'subtitle' %} subtitle{% endif %}"
                      {{ block.shopify_attributes }}
                      id="Inventory-{{ section.id }}"
                      role="status"
                    >
                      {%- if product.selected_or_first_available_variant.inventory_quantity > 0 -%}
                        {%- if product.selected_or_first_available_variant.inventory_quantity
                            <= block.settings.inventory_threshold
                        -%}
                          <span class="svg-wrapper" style="color: rgb(238, 148, 65)">
                            {{- 'icon-inventory-status.svg' | inline_asset_content -}}
                          </span>
                          {%- if block.settings.show_inventory_quantity -%}
                            {{-
                              'products.product.inventory_low_stock_show_count'
                              | t: quantity: product.selected_or_first_available_variant.inventory_quantity
                            -}}
                          {%- else -%}
                            {{- 'products.product.inventory_low_stock' | t -}}
                          {%- endif -%}
                        {%- else -%}
                          <span class="svg-wrapper" style="color: rgb(62, 214, 96)">
                            <svg class="icon icon-inventory-status">
                              <circle cx="7.5" cy="7.5" r="7.5" fill="currentColor"/><circle cx="7.5" cy="7.5" r="5" fill="currentColor" stroke="#FFF"/>
                            </svg>
                          </span>
                          {%- if block.settings.show_inventory_quantity -%}
                            {{-
                              'products.product.inventory_in_stock_show_count'
                              | t: quantity: product.selected_or_first_available_variant.inventory_quantity
                            -}}
                          {%- else -%}
                            {{- 'products.product.inventory_in_stock' | t -}}
                          {%- endif -%}
                        {%- endif -%}
                      {%- else -%}
                        {%- if product.selected_or_first_available_variant.inventory_policy == 'continue' -%}
                          <span class="svg-wrapper" style="color: rgb(62, 214, 96)">
                            <svg class="icon icon-inventory-status">
                              <circle cx="7.5" cy="7.5" r="7.5" fill="currentColor"/><circle cx="7.5" cy="7.5" r="5" fill="currentColor" stroke="#FFF"/>
                            </svg>
                          </span>
                          {{- 'products.product.inventory_out_of_stock_continue_selling' | t -}}
                        {%- else -%}
                          <span class="svg-wrapper" style="color: rgb(200, 200, 200)">
                            <svg class="icon icon-inventory-status">
                              <circle cx="7.5" cy="7.5" r="7.5" fill="currentColor"/><circle cx="7.5" cy="7.5" r="5" fill="currentColor" stroke="#FFF"/>
                            </svg>
                          </span>
                          {{- 'products.product.inventory_out_of_stock' | t -}}
                        {%- endif -%}
                      {%- endif -%}
                    </p>
                  </div>
                {% endif %}
              {%- when 'description' -%}
                {%- if product.description != blank -%}
                  <div class="product__description rte quick-add-hidden" {{ block.shopify_attributes }}>
                    <div class="{% if block.settings.show_readmore %}read-more-content{% endif %} mainproduct-description">
                      {{ product.description }}
                    </div>
                    {% if block.settings.show_readmore %}
                      <div class="read-more">{{ block.settings.button_text1 }}</div>
                      <div class="read-less">{{ block.settings.button_text2 }}</div>
                      <script>
                        document.addEventListener('DOMContentLoaded', function () {
                          var readMoreButtons = document.querySelectorAll('.read-more');
                          var readLessButtons = document.querySelectorAll('.read-less');

                          readMoreButtons.forEach(function (button) {
                            button.addEventListener('click', function (event) {
                              event.preventDefault(); // Prevent default anchor behavior
                              var readMoreContent = this.previousElementSibling;
                              if (readMoreContent && readMoreContent.classList.contains('read-more-content')) {
                                readMoreContent.style.height = '100%';
                                this.style.display = 'none';
                                this.nextElementSibling.style.display = 'inline-block';
                              }
                            });
                          });

                          readLessButtons.forEach(function (button) {
                            button.addEventListener('click', function (event) {
                              event.preventDefault(); // Prevent default anchor behavior
                              var readMoreContent = this.previousElementSibling.previousElementSibling;
                              if (readMoreContent && readMoreContent.classList.contains('read-more-content')) {
                                readMoreContent.style.height = '85px';
                                this.style.display = 'none';
                                this.previousElementSibling.style.display = 'inline-block';
                              }
                            });
                          });
                        });
                      </script>
                    {% endif %}
                  </div>
                {%- endif -%}
              {%- when 'sku' -%}
                {% if product.selected_or_first_available_variant.sku.size != 0 %}
                  <p
                    class="product__sku no-js-hidden{% if product.selected_or_first_available_variant.sku.size == 0 %} visibility-hidden{% endif %}"
                    id="Sku-{{ section.id }}"
                    role="status"
                    {{ block.shopify_attributes }}
                  >
                    <span>{{ 'products.product.sku' | t }}:</span>
                    {{- product.selected_or_first_available_variant.sku -}}
                  </p>
                {% endif %}
              {%- when 'custom_liquid' -%}
                {{ block.settings.custom_liquid }}
              {%- when 'collapsible_tab' -%}
                <div class="product__accordion accordion quick-add-hidden" {{ block.shopify_attributes }}>
                  <details id="Details-{{ block.id }}-{{ section.id }}">
                    <summary>
                      <div class="summary__title">
                        {% render 'icon-accordion', icon: block.settings.icon %}
                        <div class="h4 accordion__title inline-richtext">
                          {{ block.settings.heading | default: block.settings.page.title }}
                        </div>
                      </div>
                      {% render 'icon-caret' %}
                    </summary>
                    <div class="accordion__content rte" id="ProductAccordion-{{ block.id }}-{{ section.id }}">
                      {{ block.settings.content }}
                      {{ block.settings.page.content }}
                    </div>
                  </details>
                </div>
              {%- when 'quantity_selector' -%}
                <div
                  id="Quantity-Form-{{ section.id }}"
                  class="product-form__input product-form__quantity{% if settings.inputs_shadow_vertical_offset != 0 and settings.inputs_shadow_vertical_offset < 0 %} product-form__quantity-top{% endif %}"
                  {{ block.shopify_attributes }}
                >
                  {% comment %} TODO: enable theme-check once `item_count_for_variant` is accepted as valid filter {% endcomment %}
                  {% # theme-check-disable %}
                  {%- assign cart_qty = cart
                    | item_count_for_variant: product.selected_or_first_available_variant.id
                  -%}
                  {% # theme-check-enable %}
                  <label class="quantity__label form__label" for="Quantity-{{ section.id }}">
                    <span class="quantity__rules-cart no-js-hidden{% if cart_qty == 0 %} hidden{% endif %}">
                      {%- render 'loading-spinner' -%}
                      <span
                        >(
                        {{- 'products.product.quantity.in_cart_html' | t: quantity: cart_qty -}}
                        )</span
                      >
                    </span>
                  </label>
                  <div class="price-per-item__container">
                    <quantity-input class="quantity" data-url="{{ product.url }}" data-section="{{ section.id }}">
                      <button class="quantity__button no-js-hidden" name="minus" type="button">
                        <span class="visually-hidden">
                          {{- 'products.product.quantity.decrease' | t: product: product.title | escape -}}
                        </span>
                        {% render 'icon-minus' %}
                      </button>
                      <input
                        class="quantity__input"
                        type="number"
                        name="quantity"
                        id="Quantity-{{ section.id }}"
                        data-cart-quantity="{{ cart_qty }}"
                        data-min="{{ product.selected_or_first_available_variant.quantity_rule.min }}"
                        min="{{ product.selected_or_first_available_variant.quantity_rule.min }}"
                        {% if product.selected_or_first_available_variant.quantity_rule.max != null %}
                          data-max="{{ product.selected_or_first_available_variant.quantity_rule.max }}"
                          max="{{ product.selected_or_first_available_variant.quantity_rule.max }}"
                        {% endif %}
                        step="{{ product.selected_or_first_available_variant.quantity_rule.increment }}"
                        value="{{ product.selected_or_first_available_variant.quantity_rule.min }}"
                        form="{{ product_form_id }}"
                      >
                      <button class="quantity__button no-js-hidden" name="plus" type="button">
                        <span class="visually-hidden">
                          {{- 'products.product.quantity.increase' | t: product: product.title | escape -}}
                        </span>
                        {% render 'icon-plus' %}
                      </button>
                    </quantity-input>
                    {%- liquid
                      assign volume_pricing_array = product.selected_or_first_available_variant.quantity_price_breaks | sort: 'quantity' | reverse
                      assign current_qty_for_volume_pricing = cart_qty | plus: product.selected_or_first_available_variant.quantity_rule.min
                      if cart_qty > 0
                        assign current_qty_for_volume_pricing = cart_qty | plus: product.selected_or_first_available_variant.quantity_rule.increment
                      endif
                    -%}
                    {%- if product.quantity_price_breaks_configured? -%}
                      <price-per-item
                        class="no-js-hidden"
                        id="Price-Per-Item-{{ section.id }}"
                        data-section-id="{{ section.id }}"
                        data-variant-id="{{ product.selected_or_first_available_variant.id }}"
                      >
                        {%- if product.selected_or_first_available_variant.quantity_price_breaks.size > 0 -%}
                          {%- assign variant_price_compare = product.selected_or_first_available_variant.compare_at_price -%}
                          <div class="price-per-item">
                            {%- if variant_price_compare -%}
                              <dl class="price-per-item--current">
                                <dt class="visually-hidden">
                                  {{ 'products.product.price.regular_price' | t }}
                                </dt>
                                <dd>
                                  <s class="variant-item__old-price">
                                    {{ variant_price_compare | money_with_currency }}
                                  </s>
                                </dd>
                              </dl>
                            {%- endif -%}
                            {%- if current_qty_for_volume_pricing < volume_pricing_array.last.minimum_quantity -%}
                              {%- assign variant_price = product.selected_or_first_available_variant.price
                                | money_with_currency
                              -%}
                              <span class="price-per-item--current">
                                {{- 'products.product.volume_pricing.price_at_each' | t: price: variant_price -}}
                              </span>
                            {%- else -%}
                              {%- for price_break in volume_pricing_array -%}
                                {%- if current_qty_for_volume_pricing >= price_break.minimum_quantity -%}
                                  {%- assign price_break_price = price_break.price | money_with_currency -%}
                                  <span class="price-per-item--current">
                                    {{-
                                      'products.product.volume_pricing.price_at_each'
                                      | t: price: price_break_price
                                    -}}
                                  </span>
                                  {%- break -%}
                                {%- endif -%}
                              {%- endfor -%}
                            {%- endif -%}
                          </div>
                        {%- else -%}
                          {%- assign variant_price = product.selected_or_first_available_variant.price
                            | money_with_currency
                          -%}
                          {%- assign variant_price_compare = product.selected_or_first_available_variant.compare_at_price -%}
                          <div class="price-per-item">
                            {%- if variant_price_compare -%}
                              <dl class="price-per-item--current">
                                <dt class="visually-hidden">
                                  {{ 'products.product.price.regular_price' | t }}
                                </dt>
                                <dd>
                                  <s class="variant-item__old-price">
                                    {{ variant_price_compare | money_with_currency }}
                                  </s>
                                </dd>
                                <dt class="visually-hidden">
                                  {{ 'products.product.price.sale_price' | t }}
                                </dt>
                                <dd>
                                  <span class="price-per-item--current">
                                    {{- 'products.product.volume_pricing.price_at_each' | t: price: variant_price -}}
                                  </span>
                                </dd>
                              </dl>
                            {%- else -%}
                              <span class="price-per-item--current">
                                {{- 'products.product.volume_pricing.price_at_each' | t: price: variant_price -}}
                              </span>
                            {%- endif -%}
                          </div>
                        {%- endif -%}
                      </price-per-item>
                    {%- endif -%}
                  </div>
                  <div class="quantity__rules caption no-js-hidden" id="Quantity-Rules-{{ section.id }}">
                    {%- if product.selected_or_first_available_variant.quantity_rule.increment > 1 -%}
                      <span class="divider">
                        {{-
                          'products.product.quantity.multiples_of'
                          | t: quantity: product.selected_or_first_available_variant.quantity_rule.increment
                        -}}
                      </span>
                    {%- endif -%}
                    {%- if product.selected_or_first_available_variant.quantity_rule.min > 1 -%}
                      <span class="divider">
                        {{-
                          'products.product.quantity.minimum_of'
                          | t: quantity: product.selected_or_first_available_variant.quantity_rule.min
                        -}}
                      </span>
                    {%- endif -%}
                    {%- if product.selected_or_first_available_variant.quantity_rule.max != null -%}
                      <span class="divider">
                        {{-
                          'products.product.quantity.maximum_of'
                          | t: quantity: product.selected_or_first_available_variant.quantity_rule.max
                        -}}
                      </span>
                    {%- endif -%}
                  </div>
                  {%- if product.quantity_price_breaks_configured? -%}
                    <volume-pricing class="parent-display no-js-hidden" id="Volume-{{ section.id }}">
                      {%- if product.selected_or_first_available_variant.quantity_price_breaks.size > 0 -%}
                        <span class="caption-large">{{ 'products.product.volume_pricing.title' | t }}</span>
                        <ul class="list-unstyled no-js-hidden">
                          <li>
                            <span>{{ product.selected_or_first_available_variant.quantity_rule.min }}+</span>
                            {%- assign price = product.selected_or_first_available_variant.price
                              | money_with_currency
                            -%}
                            <span data-text="{{ 'products.product.volume_pricing.price_at_each' | t: price: variant_price }}">
                              {{- 'sections.quick_order_list.each' | t: money: price -}}
                            </span>
                          </li>
                          {%- for price_break in product.selected_or_first_available_variant.quantity_price_breaks -%}
                            {%- assign price_break_price = price_break.price | money_with_currency -%}
                            <li class="{%- if forloop.index >= 3 -%}show-more-item hidden{%- endif -%}">
                              <span>
                                {{- price_break.minimum_quantity -}}
                                <span aria-hidden="true">+</span></span
                              >
                              <span data-text="{{ 'products.product.volume_pricing.price_at_each' | t: price: price_break_price }}">
                                {{- 'sections.quick_order_list.each' | t: money: price_break_price -}}
                              </span>
                            </li>
                          {%- endfor -%}
                        </ul>
                        {%- if product.selected_or_first_available_variant.quantity_price_breaks.size >= 3 -%}
                          <show-more-button>
                            <button
                              class="button-show-more link underlined-link"
                              id="Show-More-{{ section.id }}"
                              type="button"
                            >
                              <span class="label-show-more label-text"
                                ><span aria-hidden="true">+ </span>{{ 'products.facets.show_more' | t }}
                              </span>
                            </button>
                          </show-more-button>
                        {%- endif -%}
                      {%- endif -%}
                    </volume-pricing>
                  {%- endif -%}
                </div>
                {%- assign current_variant = product.selected_or_first_available_variant -%}
                {% if product.handle == 'vita-abo' %}
                {%- assign gift_product = all_products['mellomat'] -%}
                {%- assign baseline_variant = product.variants | first -%}
                {%- assign baseline_pack_label = baseline_variant.title | split: ' x ' | first | strip -%}
                {%- assign baseline_pack_count = baseline_pack_label | plus: 0 -%}
                {%- if baseline_pack_count < 1 -%}
                  {%- assign baseline_pack_count = 1 -%}
                {%- endif -%}
                {%- assign baseline_unit_cents = baseline_variant.price | divided_by: baseline_pack_count -%}
                {%- assign default_selling_plan_id = 692445249873 -%}
                <div class="purchase">
                  <div class="dog-calculator" id="dog-calculator" data-dog-calculator>
                    <h3 class="dog-calculator__title">Die richtige Menge finden</h3>
                    <p class="dog-calculator__question">Wie viel wiegt dein Hund?</p>
                    <div class="dog-calculator__controls">
                      <button
                        type="button"
                        class="dog-calculator__btn"
                        data-weight-minus
                        aria-label="Gewicht verringern"
                      >
                        −
                      </button>
                      <span class="dog-calculator__weight">
                        <span data-field="dog-weight">25</span> kg
                      </span>
                      <button
                        type="button"
                        class="dog-calculator__btn"
                        data-weight-plus
                        aria-label="Gewicht erhöhen"
                      >
                        +
                      </button>
                    </div>
                  </div>

                  <!-- Recommendation banner -->
                  <div class="recommendation">
                    <p class="recommendation__row">Unsere Empfehlung:</p>
                    <p class="recommendation__row">
                      Dein Hund benötigt ca. <strong data-field="daily-amount">5 g</strong> pro Tag.
                    </p>
                    <p class="recommendation__row">
                      Deine Bestellung hält ca. <strong data-field="duration">4 Wochen</strong>.
                    </p>
                  </div>

                  <fieldset class="purchase-options" data-purchase-options>
                    <legend class="purchase-options__legend">Kaufoption wählen</legend>

                    <!-- OPTION 1: One-Time Purchase -->
                    <div
                      class="purchase-option purchase-option--onetime purchase-option--active"
                      data-option="onetime"
                      data-variant-selected="0"
                    >
                      <button
                        type="button"
                        class="purchase-option__header"
                        data-option-toggle
                        aria-expanded="true"
                        aria-controls="onetime-panel"
                      >
                        <span class="purchase-option__left">
                          <span class="radio" role="radio" aria-checked="true" aria-labelledby="onetime-title"></span>
                          <span class="purchase-option__title" id="onetime-title">Einmalige Lieferung</span>
                        </span>
                        <span class="purchase-option__price">
                          <span class="purchase-option__price--current" data-price-display>
                            {{- current_variant.price | money -}}
                          </span>
                        </span>
                      </button>

                      <div class="purchase-option__body">
                        <div class="purchase-option__body-inner">
                          <div
                            class="purchase-option__content"
                            id="onetime-panel"
                            role="group"
                            aria-label="Einmalige Lieferung Optionen"
                          >
                            <div
                              class="variants"
                              data-variant-group
                              data-option-names="{{ product.options_with_values | map: 'name' | join: '||' }}"
                              role="radiogroup"
                              aria-label="Packgröße wählen"
                            >
                              {% for variant in product.variants %}
                                {%- assign is_active = false -%}
                                {%- if variant.id == current_variant.id -%}
                                  {%- assign is_active = true -%}
                                {%- endif -%}
                                {%- assign pack_label = variant.title | split: ' x ' | first | strip -%}
                                {%- assign pack_count = pack_label | plus: 0 -%}
                                {%- if pack_count < 1 -%}
                                  {%- assign pack_count = 1 -%}
                                {%- endif -%}
                                {%- assign unit_price = variant.price | divided_by: pack_count -%}
                                {%- assign ot_discount = 0 -%}
                                {%- if baseline_unit_cents > unit_price -%}
                                  {%- assign ot_discount = baseline_unit_cents
                                    | minus: unit_price
                                    | times: 100.0
                                    | divided_by: baseline_unit_cents
                                    | round
                                  -%}
                                {%- endif -%}
                                <button
                                  type="button"
                                  class="variant-card {% if is_active %}variant-card--active{% endif %}"
                                  data-variant
                                  data-variant-id="{{ variant.id }}"
                                  data-option-values="{{ variant.options | join: '||' }}"
                                  data-onetime-price="{{ variant.price | money }}"
                                  data-onetime-unit-price="{{ unit_price | money }}"
                                  data-price="{{ variant.price | money }}"
                                  data-compare-at-price="{% if variant.compare_at_price > variant.price %}{{ variant.compare_at_price | money }}{% endif %}"
                                  data-available="{{ variant.available }}"
                                  role="radio"
                                  aria-checked="{{ is_active }}"
                                >
                                  {% if ot_discount > 0 %}
                                    <span class="variant-card__badge">-{{ ot_discount }} %</span>
                                  {% endif %}
                                  <span class="variant-card__pack">
                                    {{ variant.title }}
                                  </span>

                                  <span class="variant-card__price">
                                    Je {{ unit_price | money }}
                                  </span>
                                </button>
                              {% endfor %}
                            </div>

                            <div class="stock-status">
                              <span class="stock-status__dot" aria-hidden="true"></span>
                              <span>In nur 2-4 Werktagen in deinem Napf</span>
                              <button type="button" class="benefits__link" data-check-requirement style="margin-left:auto;">
                                Bedarf prüfen
                              </button>
                            </div>

                            <button type="button" class="cta" data-add-to-cart="onetime">Ab ins Warenkörbchen</button>
                            <ul class="meta-list">
                              <li class="meta-list__item">
                                <span class="meta-list__icon" aria-hidden="true">🚚</span>
                                <span>Kostenloser Versand ab 60€</span>
                              </li>
                              <li class="meta-list__item">
                                <span class="meta-list__icon" aria-hidden="true">🛡️</span>
                                <span>60 Tage Verträglichkeitsgarantie</span>
                              </li>
                            </ul>
                          </div>
                        </div>
                      </div>
                    </div>
                    <!-- OPTION 2: Subscribe & Save -->
                    {%- assign current_sub_price = current_variant.price -%}
                    {%- for allocation in current_variant.selling_plan_allocations -%}
                      {%- if allocation.selling_plan.id == default_selling_plan_id -%}
                        {%- assign current_sub_price = allocation.price -%}
                      {%- endif -%}
                    {%- endfor -%}
                    <div
                      class="purchase-option purchase-option--subscribe"
                      data-option="subscribe"
                      data-variant-selected="0"
                    >
                      <button
                        type="button"
                        class="purchase-option__header"
                        data-option-toggle
                        aria-expanded="false"
                        aria-controls="subscribe-panel"
                      >
                        <span class="purchase-option__left">
                          <span class="radio" role="radio" aria-checked="false" aria-labelledby="subscribe-title"></span>
                          <span class="purchase-option__title" id="subscribe-title">Flexibles Sparabo</span>
                        </span>
                        <span class="purchase-option__price">
                          <span class="purchase-option__price--original">
                            {{- current_variant.price | money -}}
                          </span>
                          <span class="purchase-option__price--current" data-price-display>
                            {{- current_sub_price | money -}}
                          </span>
                        </span>
                      </button>

                      <div class="purchase-option__body">
                        <div class="purchase-option__body-inner">
                          <div
                            class="purchase-option__content"
                            id="subscribe-panel"
                            role="group"
                            aria-label="Sparabo Optionen"
                          >
                            <div
                              class="variants"
                              data-variant-group
                              data-option-names="{{ product.options_with_values | map: 'name' | join: '||' }}"
                              role="radiogroup"
                              aria-label="Packgröße wählen"
                            >
                              {% for variant in product.variants %}
                                {%- assign is_active = false -%}
                                {%- if variant.id == current_variant.id -%}
                                  {%- assign is_active = true -%}
                                {%- endif -%}
                                {%- assign pack_label = variant.title | split: ' x ' | first | strip -%}
                                {%- assign pack_count = pack_label | plus: 0 -%}
                                {%- if pack_count < 1 -%}
                                  {%- assign pack_count = 1 -%}
                                {%- endif -%}
                                {%- assign sub_total = variant.price -%}
                                {%- assign sub_unit = variant.price | divided_by: pack_count -%}
                                {%- assign subscribe_prices = '' -%}
                                {%- for allocation in variant.selling_plan_allocations -%}
                                  {%- assign plan_id = allocation.selling_plan.id -%}
                                  {%- assign alloc_unit = allocation.price | divided_by: pack_count -%}
                                  {%- assign sub_discount = 0 -%}
                                  {%- if baseline_unit_cents > alloc_unit -%}
                                    {%- assign sub_discount = baseline_unit_cents
                                      | minus: alloc_unit
                                      | times: 100.0
                                      | divided_by: baseline_unit_cents
                                      | round
                                    -%}
                                  {%- endif -%}
                                  {%- if plan_id == default_selling_plan_id -%}
                                    {%- assign sub_total = allocation.price -%}
                                    {%- assign sub_unit = alloc_unit -%}
                                  {%- endif -%}
                                  {%- capture plan_entry -%}
                                    "{{ plan_id }}":{"total":{{ allocation.price | money | json }},"unit":{{ alloc_unit | money | json }},"discount":{{ sub_discount }}}
                                  {%- endcapture -%}
                                  {%- if subscribe_prices != '' -%}
                                    {%- assign subscribe_prices = subscribe_prices | append: ',' -%}
                                  {%- endif -%}
                                  {%- assign subscribe_prices = subscribe_prices | append: plan_entry -%}
                                {%- endfor -%}
                                {%- assign sub_discount = 0 -%}
                                {%- if baseline_unit_cents > sub_unit -%}
                                  {%- assign sub_discount = baseline_unit_cents
                                    | minus: sub_unit
                                    | times: 100.0
                                    | divided_by: baseline_unit_cents
                                    | round
                                  -%}
                                {%- endif -%}
                                <button
                                  type="button"
                                  class="variant-card {% if is_active %}variant-card--active{% endif %}"
                                  data-variant
                                  data-variant-id="{{ variant.id }}"
                                  data-option-values="{{ variant.options | join: '||' }}"
                                  data-pack="{{ variant.title }}"
                                  data-onetime-price="{{ variant.price | money }}"
                                  data-onetime-unit-price="{{ variant.price | divided_by: pack_count | money }}"
                                  data-price="{{ sub_total | money }}"
                                  data-subscribe-prices="{%- if subscribe_prices != '' -%}{ {{ subscribe_prices }} }{%- else -%}{}{%- endif -%}"
                                  data-compare-at-price="{{ variant.price | money }}"
                                  data-unit-price="{{ sub_unit | money }}"
                                  data-badge="{% if sub_discount > 0 %}-{{ sub_discount }} %{% endif %}"
                                  data-available="{{ variant.available }}"
                                  role="radio"
                                  aria-checked="{{ is_active }}"
                                >
                                  {% if sub_discount > 0 %}
                                    <span class="variant-card__badge" data-variant-badge>-{{ sub_discount }} %</span>
                                  {% else %}
                                    <span class="variant-card__badge" data-variant-badge hidden></span>
                                  {% endif %}
                                  <span class="variant-card__pack">{{ variant.title }}</span>
                                  <span class="variant-card__price" data-variant-unit-price>Je {{ sub_unit | money }}</span>
                                </button>
                              {% endfor %}
                            </div>

                            <div class="benefits">
                              <ul class="benefits__list">
                                <div class="heading_benefits__list">
                                  <h4>Deine Vorteile im flexiblem Sparabo:</h4>
                                  <button type="button" class="benefits__link" data-check-requirement>Bedarf prüfen</button>
                                </div>
                                <li class="benefits__item">
                                  <span class="benefits__icon" aria-hidden="true">✓</span>
                                  <span
                                    >Stressfrei versorgt bleiben &amp; exklusiven Preisvorteil mit bis zu 35&nbsp;%
                                    sichern</span
                                  >
                                </li>
                                <li class="benefits__item">
                                  <span class="benefits__icon" aria-hidden="true">✓</span>
                                  <span>Kein Risiko: Flexibel anpassbar und jederzeit kündbar</span>
                                </li>
                                <li class="benefits__item">
                                  <span class="benefits__icon" aria-hidden="true">✓</span>
                                  <span>Lieferdatum individuell überspringen oder ändern</span>
                                </li>
                              </ul>
                            </div>

                            <div class="gift">
                              <div class="gift__image" aria-hidden="true">
                                {%- if gift_product.featured_image -%}
                                  {{
                                    gift_product.featured_image
                                    | image_url: width: 96
                                    | image_tag: loading: 'lazy', alt: gift_product.title
                                  }}
                                {%- endif -%}
                              </div>
                              <div class="gift__info">
                                <span class="gift__title">
                                  {%- if gift_product != blank -%}
                                    {{ gift_product.title }}
                                    (<span class="gift__price">Kostenlos</span>)
                                  {%- else -%}
                                    Knabberhappen Probe - 300g (1 <span class="gift__price">Kostenlos</span> x 300 g)
                                  {%- endif -%}
                                </span>
                                <span class="gift__badge">Geschenkt zum Sparplan</span>
                              </div>
                            </div>

                            <div class="field">
                              <label class="field__label" for="delivery-interval">Lieferung alle</label>
                              <div class="select-wrap-info-row">
                                <div class="select-wrap">
                                  <select id="delivery-interval" class="select" data-delivery-interval>
                                    <option value="692445249873" data-weeks="4" selected>Lieferung alle 4 Wochen</option>
                                    <option value="692445282641" data-weeks="6">Lieferung alle 6 Wochen</option>
                                    <option value="692445315409" data-weeks="8">Lieferung alle 8 Wochen</option>
                                  </select>
                                </div>
                                <div class="info-row">
                                  <span class="info-row__icon" aria-hidden="true">i</span>
                                  <span>So funktioniert's</span>
                                </div>
                              </div>
                            </div>

                            <div class="stock-status">
                              <span class="stock-status__dot" aria-hidden="true"></span>
                              <span>In nur 2-4 Werktagen in deinem Napf</span>
                            </div>

                            <button type="button" class="cta" data-add-to-cart="subscribe">Ab ins Warenkörbchen</button>
                            <ul class="meta-list">
                              <li class="meta-list__item">
                                <span class="meta-list__icon" aria-hidden="true">🚚</span>
                                <span>Kostenloser Versand ab 60€</span>
                              </li>
                              <li class="meta-list__item">
                                <span class="meta-list__icon" aria-hidden="true">🛡️</span>
                                <span>60 Tage Verträglichkeitsgarantie</span>
                              </li>
                            </ul>
                          </div>
                        </div>
                      </div>
                    </div>
                  </fieldset>
                </div>
                {% endif %}
              {%- when 'comparewishlist' -%}
                <div class="product_page_copm_wish product-hover">
                  {% if product %}
                    {% render 'button-wishlist-compare', product: product %}
                  {% endif %}
                </div>
              {%- when 'popup' -%}
                <modal-opener
                  class="product-popup-modal__opener no-js-hidden quick-add-hidden"
                  data-modal="#PopupModal-{{ block.id }}"
                  {{ block.shopify_attributes }}
                >
                  <button
                    id="ProductPopup-{{ block.id }}"
                    class="product-popup-modal__button link"
                    type="button"
                    aria-haspopup="dialog"
                  >
                    {% render 'icon-scale' %}
                    {{ block.settings.text | default: block.settings.page.title }}
                  </button>
                </modal-opener>
                <a href="{{ block.settings.page.url }}" class="product-popup-modal__button link no-js">
                  {{- block.settings.text -}}
                </a>
              {%- when 'share' -%}
                {% assign share_url = product.selected_variant.url | default: product.url | prepend: request.origin %}
                {% render 'share-button', block: block, share_link: share_url %}

              {%- when 'variant_picker' -%}
                {% render 'product-variant-picker', product: product, block: block, product_form_id: product_form_id %}
              {%- when 'buy_buttons' -%}
                {%- render 'buy-buttons',
                  block: block,
                  product: product,
                  product_form_id: product_form_id,
                  section_id: section.id,
                  show_pickup_availability: true
                -%}
                {% if block.settings.collection_show %}
                  <div class="product_category_feature">
                    <div class="feature-category">
                      <label class="form__label">{{ 'products.product.product_category' | t }}</label>
                      {% for collection in product.collections -%}
                        {{ collection.title | link_to: collection.url, class: 'main_collection' }}
                      {% endfor -%}
                    </div>
                  </div>
                {% endif %}
              {%- when 'rating' -%}
                {%- if product.metafields.reviews.rating.value != blank -%}
                  {% liquid
                    assign rating_decimal = 0
                    assign decimal = product.metafields.reviews.rating.value.rating | modulo: 1
                    if decimal >= 0.3 and decimal <= 0.7
                      assign rating_decimal = 0.5
                    elsif decimal > 0.7
                      assign rating_decimal = 1
                    endif
                  %}
                  <div
                    class="rating"
                    role="img"
                    aria-label="{{ 'accessibility.star_reviews_info' | t: rating_value: product.metafields.reviews.rating.value, rating_max: product.metafields.reviews.rating.value.scale_max }}"
                  >
                    <span
                      aria-hidden="true"
                      class="rating-star"
                      style="--rating: {{ product.metafields.reviews.rating.value.rating | floor }}; --rating-max: {{ product.metafields.reviews.rating.value.scale_max }}; --rating-decimal: {{ rating_decimal }};"
                    ></span>
                  </div>
                  <p class="rating-text caption">
                    <span aria-hidden="true">
                      {{- product.metafields.reviews.rating.value }} /
                      {{ product.metafields.reviews.rating.value.scale_max -}}
                    </span>
                  </p>
                  <p class="rating-count caption">
                    <span aria-hidden="true">({{ product.metafields.reviews.rating_count }})</span>
                    <span class="visually-hidden">
                      {{- product.metafields.reviews.rating_count }}
                      {{ 'accessibility.total_reviews' | t -}}
                    </span>
                  </p>
                {%- endif -%}
              {%- when 'complementary' -%}
                <product-recommendations
                  class="complementary-products quick-add-hidden no-js-hidden{% if block.settings.make_collapsible_row %} is-accordion{% endif %}{% if block.settings.enable_quick_add %} complementary-products-contains-quick-add{% endif %}"
                  data-url="{{ routes.product_recommendations_url }}?section_id={{ section.id }}&product_id={{ product.id }}&limit={{ block.settings.product_list_limit }}&intent=complementary"
                >
                  {%- if recommendations.performed and recommendations.products_count > 0 -%}
                    <aside
                      aria-label="{{ 'accessibility.complementary_products' | t }}"
                      {{ block.shopify_attributes -}}
                      {% if block.settings.make_collapsible_row %}
                        class="product__accordion accordion"
                      {% endif %}
                    >
                      <div class="complementary-products__container">
                        {%- if block.settings.make_collapsible_row -%}
                          <details id="Details-{{ block.id }}-{{ section.id }}" open>
                            <summary>
                        {%- endif %}
                        <div class="summary__title">
                          {%- if block.settings.make_collapsible_row -%}
                            {% render 'icon-accordion', icon: block.settings.icon %}
                            <div class="h4 accordion__title">{{ block.settings.block_heading }}</div>
                          {%- else -%}
                            <div class="h3 accordion__title">{{ block.settings.block_heading }}</div>
                          {%- endif -%}
                        </div>
                        {%- if block.settings.make_collapsible_row -%}
                          {% render 'icon-caret' %}
                          </summary>
                        {%- endif -%}
                        <slideshow-component class="slider-mobile-gutter">
                          {%- assign number_of_slides = recommendations.products_count
                            | plus: 0.0
                            | divided_by: block.settings.products_per_page
                            | ceil
                          -%}
                          <div
                            id="Slider-{{ block.id }}"
                            class="contains-card contains-card--product complementary-slider grid grid--1-col slider slider--everywhere"
                            role="list"
                            {% if number_of_slides > 1 %}
                              aria-label="{{ 'general.slider.name' | t }}"
                            {% endif %}
                          >
                            {%- for i in (1..number_of_slides) -%}
                              <div
                                id="Slide-{{ block.id }}-{{ forloop.index }}"
                                class="complementary-slide complementary-slide--{{ settings.card_style }} grid__item slider__slide slideshow__slide"
                                tabindex="-1"
                                role="group"
                                {% if number_of_slides > 1 %}
                                  aria-roledescription="{{ 'sections.slideshow.slide' | t }}"
                                  aria-label="{{ forloop.index }} {{ 'general.slider.of' | t }} {{ forloop.length }}"
                                {% endif %}
                              >
                                <ul class="list-unstyled" role="list">
                                  {%- for product in recommendations.products
                                    limit: block.settings.products_per_page
                                    offset: continue
                                  -%}
                                    <li>
                                      {% render 'card-product',
                                        card_product: product,
                                        media_aspect_ratio: block.settings.image_ratio,
                                        show_secondary_image: false,
                                        lazy_load: false,
                                        show_quick_add: block.settings.enable_quick_add,
                                        section_id: section.id,
                                        horizontal_class: true,
                                        horizontal_quick_add: true
                                      %}
                                    </li>
                                  {%- endfor -%}
                                </ul>
                              </div>
                            {%- endfor -%}
                          </div>
                          {%- if number_of_slides > 1 -%}
                            <div class="slider-buttons no-js-hidden">
                              <button
                                type="button"
                                class="slider-button slider-button--prev"
                                name="previous"
                                aria-label="{{ 'general.slider.previous_slide' | t }}"
                              >
                                {% render 'icon-caret' %}
                              </button>
                              <div class="slider-counter slider-counter--{{ block.settings.pagination_style }}{% if block.settings.pagination_style == 'counter' or block.settings.pagination_style == 'numbers' %} caption{% endif %}">
                                {%- if block.settings.pagination_style == 'counter' -%}
                                  <span class="slider-counter--current">1</span>
                                  <span aria-hidden="true"> / </span>
                                  <span class="visually-hidden">{{ 'general.slider.of' | t }}</span>
                                  <span class="slider-counter--total">{{ number_of_slides }}</span>
                                {%- else -%}
                                  <div class="slideshow__control-wrapper">
                                    {%- for i in (1..number_of_slides) -%}
                                      <button
                                        class="slider-counter__link slider-counter__link--{{ block.settings.pagination_style }} link"
                                        aria-label="{{ 'sections.slideshow.load_slide' | t }} {{ forloop.index }} {{ 'general.slider.of' | t }} {{ forloop.length }}"
                                        aria-controls="Slider-{{ block.id }}"
                                      >
                                        {%- if block.settings.pagination_style == 'numbers' -%}
                                          {{ forloop.index -}}
                                        {%- else -%}
                                          <span class="dot"></span>
                                        {%- endif -%}
                                      </button>
                                    {%- endfor -%}
                                  </div>
                                {%- endif -%}
                              </div>
                              <button
                                type="button"
                                class="slider-button slider-button--next"
                                name="next"
                                aria-label="{{ 'general.slider.next_slide' | t }}"
                              >
                                {% render 'icon-caret' %}
                              </button>
                            </div>
                          {%- endif -%}
                        </slideshow-component>
                        {%- if block.settings.make_collapsible_row -%}
                          </details>
                        {%- endif -%}
                      </div>
                    </aside>
                  {%- endif -%}
                  {{ 'component-card.css' | asset_url | stylesheet_tag }}
                  {{ 'component-complementary-products.css' | asset_url | stylesheet_tag }}
                  {%- if block.settings.enable_quick_add -%}
                    {{ 'quick-add.css' | asset_url | stylesheet_tag }}
                    <script src="{{ 'quick-add.js' | asset_url }}" defer="defer"></script>
                  {%- endif -%}
                </product-recommendations>
              {%- when 'icon-with-text' -%}
                {% render 'icon-with-text', block: block %}
            {%- endcase -%}
          {%- endfor -%}
          <a href="{{ product.url }}" class="link product__view-details animate-arrow">
            {{ 'products.product.view_full_details' | t }}
            {% render 'icon-arrow' %}
          </a>
        </product-info>
      </div>
      {%- if section.settings.media_position == 'right' -%}
        {% comment %} Duplicate gallery to display after product content on tablet/desktop breakpoint {% endcomment %}
        <div class="grid__item product__media-wrapper small-hide">
          {% render 'product-media-gallery', variant_images: variant_images, is_duplicate: true %}
        </div>
      {%- endif -%}
    </div>

    {% render 'product-media-modal', variant_images: variant_images %}

    {% assign popups = section.blocks | where: 'type', 'popup' %}
    {%- for block in popups -%}
      <modal-dialog id="PopupModal-{{ block.id }}" class="product-popup-modal" {{ block.shopify_attributes }}>
        <div
          role="dialog"
          aria-label="{{ block.settings.text }}"
          aria-modal="true"
          class="product-popup-modal__content"
          tabindex="-1"
        >
          <button
            id="ModalClose-{{ block.id }}"
            type="button"
            class="product-popup-modal__toggle"
            aria-label="{{ 'accessibility.close' | t }}"
          >
            {% render 'icon-close' %}
          </button>
          <div class="product-popup-modal__content-info">
            <div class="h2 theme_main_heading">{{ block.settings.page.title }}</div>
            {% if block.settings.page.content != blank and product.metafields.custom.size_chart == blank %}
              {{ block.settings.page.content }}
            {% else %}
              {% if product.metafields.custom.size_chart.value != blank -%}
                {{- product.metafields.custom.size_chart | image_url: width: 350 | image_tag }}
              {% endif %}
            {% endif %}
          </div>
        </div>
      </modal-dialog>
    {%- endfor -%}

    {%- if product.media.size > 0 -%}
      <script src="{{ 'product-modal.js' | asset_url }}" defer="defer"></script>
      <script src="{{ 'media-gallery.js' | asset_url }}" defer="defer"></script>
    {%- endif -%}

    {%- if first_3d_model -%}
      <script type="application/json" id="ProductJSON-{{ product.id }}">
        {{ product.media | where: 'media_type', 'model' | json }}
      </script>
      <script src="{{ 'product-model.js' | asset_url }}" defer></script>
    {%- endif -%}

    <script>
      document.addEventListener('DOMContentLoaded', function () {
        function isIE() {
          const ua = window.navigator.userAgent;
          const msie = ua.indexOf('MSIE ');
          const trident = ua.indexOf('Trident/');

          return msie > 0 || trident > 0;
        }

        if (!isIE()) return;
        const hiddenInput = document.querySelector('#{{ product_form_id }} input[name="id"]');
        const noScriptInputWrapper = document.createElement('div');
        const variantSwitcher =
          document.querySelector('variant-radios[data-section="{{ section.id }}"]') ||
          document.querySelector('variant-selects[data-section="{{ section.id }}"]');
        noScriptInputWrapper.innerHTML = document.querySelector(
          '.product-form__noscript-wrapper-{{ section.id }}'
        ).textContent;
        variantSwitcher.outerHTML = noScriptInputWrapper.outerHTML;

        document.querySelector('#Variants-{{ section.id }}').addEventListener('change', function (event) {
          hiddenInput.value = event.currentTarget.value;
        });
      });
    </script>

    {%- liquid
      if product.selected_or_first_available_variant.featured_media
        assign seo_media = product.selected_or_first_available_variant.featured_media
      else
        assign seo_media = product.featured_media
      endif
    -%}

    <script type="application/ld+json">
      {
        "@context": "http://schema.org/",
        "@type": "Product",
        "name": {{ product.title | json }},
        "url": {{ request.origin | append: product.url | json }},
        {% if seo_media -%}
          "image": [
            {{ seo_media | image_url: width: 1920 | prepend: "https:" | json }}
          ],
        {%- endif %}
        "description": {{ product.description | strip_html | json }},
        {% if product.selected_or_first_available_variant.sku != blank -%}
          "sku": {{ product.selected_or_first_available_variant.sku | json }},
        {%- endif %}
        "brand": {
          "@type": "Brand",
          "name": {{ product.vendor | json }}
        },
        "offers": [
          {%- for variant in product.variants -%}
            {
              "@type" : "Offer",
              {%- if variant.sku != blank -%}
                "sku": {{ variant.sku | json }},
              {%- endif -%}
              {%- if variant.barcode.size == 12 -%}
                "gtin12": {{ variant.barcode }},
              {%- endif -%}
              {%- if variant.barcode.size == 13 -%}
                "gtin13": {{ variant.barcode }},
              {%- endif -%}
              {%- if variant.barcode.size == 14 -%}
                "gtin14": {{ variant.barcode }},
              {%- endif -%}
              "availability" : "http://schema.org/{% if variant.available %}InStock{% else %}OutOfStock{% endif %}",
              "price" : {{ variant.price | divided_by: 100.00 | json }},
              "priceCurrency" : {{ cart.currency.iso_code | json }},
              "url" : {{ request.origin | append: variant.url | json }}
            }{% unless forloop.last %},{% endunless %}
          {%- endfor -%}
        ]
      }
    </script>
  </div>

  {% if section.settings.show_stick_cart %}
    {% if template contains 'product' %}
      {% assign featured_image = product.images.first | default: product.featured_image %}
      <div class="sticky_cart">
        <div class="product-details page-width">
          <div class="prd-details prd-details-left">
            <div class="prd-image-title">
              <div class="sticky-product-images">
                <img src="{{ featured_image | img_url: "300x" }}" alt="product-img">
              </div>
              <div class="sticky-page">
                <div class="h3">{{ product.title }}</div>
                <div class="no-js-hidden" id="price-{{ section.id }}" role="status" {{ block.shopify_attributes }}>
                  {%- render 'price', product: product, use_variant: true, price_class: 'price--large' -%}
                </div>
                {%- if shop.taxes_included or shop.shipping_policy.body != blank -%}
                  <div class="product__tax caption rte">
                    {%- if shop.taxes_included -%}
                      {{ 'products.product.include_taxes' | t }}
                    {%- endif -%}
                    {%- if shop.shipping_policy.body != blank -%}
                      {{ 'products.product.shipping_policy_html' | t: link: shop.shipping_policy.url }}
                    {%- endif -%}
                  </div>
                {%- endif -%}
                <div {{ block.shopify_attributes }}>
                  {%- assign product_form_installment_id = 'product-form-installment-' | append: section.id -%}
                  {%- form 'product', product, id: product_form_installment_id, class: 'installment caption-large' -%}
                    <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
                    {{ form | payment_terms }}
                  {%- endform -%}
                </div>
              </div>
            </div>
          </div>
          <div class="prd-details prd-details-right">
            <div class="prd-image-price-cart">
              <product-form class="product-form">
                <div class="product-form__error-message-wrapper" role="alert" hidden>
                  <svg
                    aria-hidden="true"
                    focusable="false"
                    role="presentation"
                    class="icon icon-error"
                    viewBox="0 0 13 13"
                  >
                    <circle cx="6.5" cy="6.50049" r="5.5" stroke="white" stroke-width="2"/>
                    <circle cx="6.5" cy="6.5" r="5.5" fill="#EB001B" stroke="#EB001B" stroke-width="0.7"/>
                    <path d="M5.87413 3.52832L5.97439 7.57216H7.02713L7.12739 3.52832H5.87413ZM6.50076 9.66091C6.88091 9.66091 7.18169 9.37267 7.18169 9.00504C7.18169 8.63742 6.88091 8.34917 6.50076 8.34917C6.12061 8.34917 5.81982 8.63742 5.81982 9.00504C5.81982 9.37267 6.12061 9.66091 6.50076 9.66091Z" fill="white"/>
                    <path d="M5.87413 3.17832H5.51535L5.52424 3.537L5.6245 7.58083L5.63296 7.92216H5.97439H7.02713H7.36856L7.37702 7.58083L7.47728 3.537L7.48617 3.17832H7.12739H5.87413ZM6.50076 10.0109C7.06121 10.0109 7.5317 9.57872 7.5317 9.00504C7.5317 8.43137 7.06121 7.99918 6.50076 7.99918C5.94031 7.99918 5.46982 8.43137 5.46982 9.00504C5.46982 9.57872 5.94031 10.0109 6.50076 10.0109Z" fill="white" stroke="#EB001B" stroke-width="0.7">
                  </svg>
                  <span class="product-form__error-message"></span>
                </div>

                {%- form 'product',
                  product,
                  id: product_form_id,
                  class: 'form',
                  novalidate: 'novalidate',
                  data-type: 'add-to-cart-form'
                -%}
                  {% assign variant = product.selected_or_first_available_variant %}
                  {%- if product.variants.size > 1 -%}
                    <select name="id" class="sticky-select theme_buttons button">
                      {% for variant in product.variants %}
                        {% assign selected = false %}
                        {%- if variant.id == product.selected_or_first_available_variant.id -%}
                          {% assign selected = true %}
                        {%- endif -%}
                        <option
                          value="{{ variant.id }}"
                          {% if selected %}
                            selected="true"
                          {% endif %}
                        >
                          {{ variant.title }} -
                          {% if settings.currency_code_enabled %}
                            {{ variant.price | money_with_currency }}
                          {% else %}
                            {{ variant.price | money }}
                          {% endif %}
                        </option>
                      {% endfor %}
                    </select>
                  {%- else -%}
                    <input
                      type="hidden"
                      name="id"
                      value="{{ product.selected_or_first_available_variant.id }}"
                      disabled
                    >
                  {%- endif -%}
                  <quantity-input class="quantity">
                    <button class="quantity__button no-js-hidden" name="minus" type="button">
                      <span class="visually-hidden">
                        {{- 'products.product.quantity.decrease' | t: product: product.title | escape -}}
                      </span>
                      {% render 'icon-minus' %}
                    </button>
                    <input
                      type="number"
                      name="quantity"
                      id="quantity"
                      value="1"
                      class="sticky-quantity quantity__input theme_buttons"
                    >
                    <button class="quantity__button no-js-hidden" name="plus" type="button">
                      <span class="visually-hidden">
                        {{- 'products.product.quantity.increase' | t: product: product.title | escape -}}
                      </span>
                      {% render 'icon-plus' %}
                    </button>
                  </quantity-input>
                  <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}" disabled>
                  <div class="product-form__buttons">
                    <button
                      id="{{ product_form_id }}-submit"
                      type="submit"
                      name="add"
                      class="quick-add__submit button button--full-width button--secondary{% if horizontal_quick_add %} card--horizontal__quick-add{% endif %}"
                      aria-haspopup="dialog"
                      aria-labelledby="{{ product_form_id }}-submit title-{{ section_id }}-{{ product.id }}"
                      aria-live="polite"
                      data-sold-out-message="true"
                      {% if product.selected_or_first_available_variant.available == false %}
                        disabled
                      {% endif %}
                    >
                      <span>
                        {%- if product.selected_or_first_available_variant.available -%}
                          {{ 'products.product.add_to_cart' | t }}
                        {%- else -%}
                          {{ 'products.product.sold_out' | t }}
                        {%- endif -%}
                      </span>
                      <span class="sold-out-message hidden">
                        {{ 'products.product.sold_out' | t }}
                      </span>
                      {%- render 'loading-spinner' -%}
                    </button>
                    {%- if block.settings.show_dynamic_checkout -%}
                      {{ form | payment_button }}
                    {%- endif -%}
                  </div>

                  <div class="sticky_cart_close">
                    {% render 'icon-close' %}
                  </div>
                {%- endform -%}
              </product-form>
            </div>
          </div>
        </div>
      </div>
    {% endif %}

    <script>
      document.addEventListener('DOMContentLoaded', function () {
        window.addEventListener('scroll', function (event) {
          var height = window.innerHeight;
          var scroll = window.scrollY;
          var scrollHeight = document.body.scrollHeight;
          var scrollTop = this.scrollY;

          var stickyCart = document.querySelector('.sticky_cart');

          if (scroll > 1500) {
            stickyCart.classList.add('show');
          } else {
            stickyCart.classList.remove('show');
          }

          if (scrollTop + height >= scrollHeight) {
            stickyCart.classList.remove('show');
          }
        });
      });

      document.addEventListener('DOMContentLoaded', function () {
        var stickyCartClose = document.querySelector('.sticky_cart_close');
        stickyCartClose.addEventListener('click', function () {
          var stickyCartShow = document.querySelector('.sticky_cart.show');
          if (stickyCartShow) {
            stickyCartShow.style.bottom = '-275px';
          }
        });
      });
    </script>
  {% endif %}
  <script>
    (function () {
      'use strict';

      var optionsRoot = document.querySelector('[data-purchase-options]');
      if (!optionsRoot) return;

      var purchaseRoot = optionsRoot.closest('.purchase');
      var options = Array.prototype.slice.call(optionsRoot.querySelectorAll('.purchase-option'));

      var WEIGHT_MIN = 1;
      var WEIGHT_MAX = 80;
      var WEIGHT_DEFAULT = 25;
      var GRAMS_PER_KG = 0.2;
      var ONETIME_DURATION_WEEKS = 4;
      var currentWeight = WEIGHT_DEFAULT;

      var calculatorEl = purchaseRoot ? purchaseRoot.querySelector('[data-dog-calculator]') : null;
      var weightDisplayEl = calculatorEl ? calculatorEl.querySelector('[data-field="dog-weight"]') : null;
      var dailyAmountEl = purchaseRoot ? purchaseRoot.querySelector('[data-field="daily-amount"]') : null;
      var durationEl = purchaseRoot ? purchaseRoot.querySelector('[data-field="duration"]') : null;

      function formatGrams(grams) {
        var rounded = Math.round(grams * 10) / 10;
        if (Math.abs(rounded - Math.round(rounded)) < 0.05) {
          return Math.round(rounded) + ' g';
        }
        return rounded.toFixed(1).replace('.', ',') + ' g';
      }

      function getDeliveryWeeks() {
        var activeOption = optionsRoot.querySelector('.purchase-option--active');
        if (activeOption && activeOption.getAttribute('data-option') === 'subscribe') {
          var select = activeOption.querySelector('[data-delivery-interval]');
          if (select && select.selectedOptions.length) {
            var weeks = parseInt(select.selectedOptions[0].getAttribute('data-weeks'), 10);
            if (!isNaN(weeks)) return weeks;
          }
        }
        return ONETIME_DURATION_WEEKS;
      }

      function updateRecommendation() {
        var dailyGrams = currentWeight * GRAMS_PER_KG;
        if (dailyAmountEl) dailyAmountEl.textContent = formatGrams(dailyGrams);
        if (durationEl) durationEl.textContent = getDeliveryWeeks() + ' Wochen';
        if (weightDisplayEl) weightDisplayEl.textContent = String(currentWeight);
      }

      function setWeight(nextWeight) {
        currentWeight = Math.min(WEIGHT_MAX, Math.max(WEIGHT_MIN, nextWeight));
        updateRecommendation();
      }

      function scrollToCalculator() {
        if (!calculatorEl) return;
        calculatorEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
        calculatorEl.classList.add('dog-calculator--highlight');
        window.setTimeout(function () {
          calculatorEl.classList.remove('dog-calculator--highlight');
        }, 1500);
      }

      if (calculatorEl) {
        var minusBtn = calculatorEl.querySelector('[data-weight-minus]');
        var plusBtn = calculatorEl.querySelector('[data-weight-plus]');

        if (minusBtn) {
          minusBtn.addEventListener('click', function () {
            setWeight(currentWeight - 1);
          });
        }

        if (plusBtn) {
          plusBtn.addEventListener('click', function () {
            setWeight(currentWeight + 1);
          });
        }
      }

      if (purchaseRoot) {
        purchaseRoot.querySelectorAll('[data-check-requirement]').forEach(function (btn) {
          btn.addEventListener('click', scrollToCalculator);
        });
      }

      updateRecommendation();

      // ---- Native Shopify element references (hidden via CSS, kept for real cart/variant logic) ----
      var nativeVariantPicker =
        document.querySelector('variant-radios[data-section="{{ section.id }}"]') ||
        document.querySelector('variant-selects[data-section="{{ section.id }}"]');

      var nativeForm = document.querySelector('#{{ product_form_id }}');
      var nativeSubmitButton = nativeForm
        ? nativeForm.querySelector('.product-form__submit, button[name="add"]')
        : document.querySelector('.product__info-wrapper .product-form__submit');

      // Seal Subscriptions app widget (real purchase-option + delivery-interval controls)
      var nativeSellingPlanRoot = document.querySelector('.sls-purchase-options-container');

      function cssEscape(value) {
        if (window.CSS && CSS.escape) return CSS.escape(value);
        return String(value).replace(/([^a-zA-Z0-9_-])/g, '\\$1');
      }

      // Sync a selected custom variant card into the real (hidden) Shopify variant picker
      function syncNativeVariant(group, variantBtn) {
        if (!nativeVariantPicker) return;

        var optionNames = (group.getAttribute('data-option-names') || '').split('||').filter(Boolean);
        var optionValues = (variantBtn.getAttribute('data-option-values') || '').split('||');
        if (!optionNames.length || !optionValues.length) return;

        optionNames.forEach(function (name, i) {
          var value = optionValues[i];
          if (!value) return;

          var radio = nativeVariantPicker.querySelector(
            'input[type="radio"][name="' + cssEscape(name) + '"][value="' + cssEscape(value) + '"]'
          );
          if (radio) {
            if (!radio.checked) {
              radio.checked = true;
              radio.dispatchEvent(new Event('change', { bubbles: true }));
            }
            return;
          }

          var select = nativeVariantPicker.querySelector('select[name="' + cssEscape(name) + '"]');
          if (select && select.value !== value) {
            select.value = value;
            select.dispatchEvent(new Event('change', { bubbles: true }));
          }
        });
      }

      // Sync the selected purchase option (onetime / subscribe) into the real Seal Subscriptions widget
      function syncNativePurchaseOption(purchaseOption) {
        if (!nativeSellingPlanRoot) return;

        // One Time & Subscribe radios
        const oneTimeRadio = nativeSellingPlanRoot.querySelector('input.sls-option[data-type="one_time"]');

        const subscribeRadio = nativeSellingPlanRoot.querySelector('input.sls-option[data-type="subscription"]');

        // Hidden selling plan input
        const sellingPlanInput = nativeSellingPlanRoot.querySelector('input[name="selling_plan"]');

        if (purchaseOption === 'subscribe') {
          if (subscribeRadio) {
            subscribeRadio.checked = true;

            subscribeRadio.dispatchEvent(
              new Event('change', {
                bubbles: true,
              })
            );

            subscribeRadio.click();
          }
        } else {
          if (oneTimeRadio) {
            oneTimeRadio.checked = true;

            oneTimeRadio.dispatchEvent(
              new Event('change', {
                bubbles: true,
              })
            );

            oneTimeRadio.click();
          }

          // Remove selling plan for one time purchase
          if (sellingPlanInput) {
            sellingPlanInput.value = '';
          }
        }

        // Give Seal App time to update its UI
        setTimeout(function () {
          nativeSellingPlanRoot.querySelectorAll('.sls-option-container').forEach(function (item) {
            item.classList.remove('sls-active');
          });

          const activeRadio = purchaseOption === 'subscribe' ? subscribeRadio : oneTimeRadio;

          if (activeRadio) {
            const activeContainer = activeRadio.closest('.sls-option-container');

            if (activeContainer) {
              activeContainer.classList.add('sls-active');
            }
          }
        }, 100);
      }

      // Sync the selected custom delivery interval (weeks) into the real Seal Subscriptions interval select
      function syncNativeDeliveryInterval(weeksValue) {
        if (!nativeSellingPlanRoot) return;

        // Seal App select
        const appSelect = nativeSellingPlanRoot.querySelector('.sls-select');

        if (!appSelect) return;

        // Set selected value
        appSelect.value = weeksValue;

        // Trigger events so Seal App updates hidden selling_plan input
        appSelect.dispatchEvent(
          new Event('change', {
            bubbles: true,
          })
        );

        appSelect.dispatchEvent(
          new Event('input', {
            bubbles: true,
          })
        );

        // Extra safety for some themes/apps
        setTimeout(function () {
          appSelect.dispatchEvent(
            new Event('change', {
              bubbles: true,
            })
          );
        }, 100);
      }

      function setActiveOption(targetOption) {
        options.forEach(function (option) {
          var isActive = option === targetOption;
          var toggle = option.querySelector('[data-option-toggle]');
          var radio = option.querySelector('.radio');

          option.classList.toggle('purchase-option--active', isActive);
          toggle.setAttribute('aria-expanded', String(isActive));
          radio.setAttribute('aria-checked', String(isActive));
        });

        syncNativePurchaseOption(targetOption.getAttribute('data-option'));
        updateRecommendation();

        var activeVariant = targetOption.querySelector('.variant-card--active');
        if (activeVariant) {
          updatePrice(activeVariant, targetOption);
        }
      }

      function getSelectedPlanId() {
        var activeOption = optionsRoot.querySelector('.purchase-option--active');
        if (!activeOption || activeOption.getAttribute('data-option') !== 'subscribe') return null;
        var select = activeOption.querySelector('[data-delivery-interval]');
        return select ? select.value : null;
      }

      function parseSubscribePrices(variantBtn) {
        var raw = variantBtn.getAttribute('data-subscribe-prices');
        if (!raw) return {};
        try {
          return JSON.parse(raw);
        } catch (error) {
          return {};
        }
      }

      function getVariantPricing(variantBtn, option) {
        var isSubscribe = option.getAttribute('data-option') === 'subscribe';

        if (!isSubscribe) {
          return {
            price: variantBtn.getAttribute('data-onetime-price') || variantBtn.getAttribute('data-price'),
            comparePrice: '',
          };
        }

        var planId = getSelectedPlanId();
        var planPrices = parseSubscribePrices(variantBtn);
        var planPrice = planId && planPrices[planId] ? planPrices[planId] : null;

        return {
          price: planPrice ? planPrice.total : variantBtn.getAttribute('data-price'),
          comparePrice: variantBtn.getAttribute('data-onetime-price') || variantBtn.getAttribute('data-compare-at-price') || '',
          unitPrice: planPrice ? planPrice.unit : variantBtn.getAttribute('data-unit-price'),
          discount: planPrice ? planPrice.discount : 0,
        };
      }

      function updateVariantCardPricing(variantBtn, option) {
        if (option.getAttribute('data-option') !== 'subscribe') return;

        var pricing = getVariantPricing(variantBtn, option);
        var unitEl = variantBtn.querySelector('[data-variant-unit-price]');
        var badgeEl = variantBtn.querySelector('[data-variant-badge]');

        if (unitEl && pricing.unitPrice) {
          unitEl.textContent = 'Je ' + pricing.unitPrice;
        }

        if (badgeEl) {
          if (pricing.discount > 0) {
            badgeEl.textContent = '-' + pricing.discount + ' %';
            badgeEl.hidden = false;
          } else {
            badgeEl.hidden = true;
          }
        }
      }

      function updateAllSubscribeVariantCards() {
        var subscribeOption = optionsRoot.querySelector('.purchase-option--subscribe');
        if (!subscribeOption) return;

        subscribeOption.querySelectorAll('[data-variant]').forEach(function (btn) {
          updateVariantCardPricing(btn, subscribeOption);
        });
      }

      function updatePrice(variantBtn, option) {
        var pricing = getVariantPricing(variantBtn, option);

        // Header current price
        var priceDisplay = option.querySelector('[data-price-display]');
        if (priceDisplay) {
          priceDisplay.textContent = pricing.price;
        }

        // Header original / compare-at price
        var originalDisplay = option.querySelector('.purchase-option__price--original');
        if (originalDisplay) {
          if (option.getAttribute('data-option') === 'subscribe' && pricing.comparePrice) {
            originalDisplay.textContent = pricing.comparePrice;
            originalDisplay.style.display = '';
          } else {
            originalDisplay.style.display = 'none';
          }
        }

        updateVariantCardPricing(variantBtn, option);

        // ATC Price (supports multiple elements)
        document.querySelectorAll('.section-store--atc-price').forEach(function (el) {
          el.textContent = pricing.price;
        });
      }

      function selectVariant(option, variantBtn) {
        var group = option.querySelector('[data-variant-group]');
        var variants = Array.prototype.slice.call(group.querySelectorAll('[data-variant]'));

        variants.forEach(function (btn) {
          var isActive = btn === variantBtn;
          btn.classList.toggle('variant-card--active', isActive);
          btn.setAttribute('aria-checked', String(isActive));
        });

        updatePrice(variantBtn, option);
        syncNativeVariant(group, variantBtn);

        option.setAttribute('data-variant-selected', String(variants.indexOf(variantBtn)));
        option.setAttribute('data-selected-variant-id', variantBtn.getAttribute('data-variant-id'));
      }

      options.forEach(function (option) {
        var toggle = option.querySelector('[data-option-toggle]');

        toggle.addEventListener('click', function () {
          if (option.classList.contains('purchase-option--active')) return;
          setActiveOption(option);
        });

        toggle.addEventListener('keydown', function (e) {
          if (e.key === 'Enter' || e.key === ' ') {
            e.preventDefault();
            toggle.click();
          }
        });

        var group = option.querySelector('[data-variant-group]');
        if (!group) return;

        var variantButtons = Array.prototype.slice.call(group.querySelectorAll('[data-variant]'));

        variantButtons.forEach(function (btn, index) {
          btn.addEventListener('click', function () {
            selectVariant(option, btn);
          });

          btn.addEventListener('keydown', function (e) {
            var nextIndex = null;

            if (e.key === 'ArrowRight' || e.key === 'ArrowDown') {
              nextIndex = (index + 1) % variantButtons.length;
            }

            if (e.key === 'ArrowLeft' || e.key === 'ArrowUp') {
              nextIndex = (index - 1 + variantButtons.length) % variantButtons.length;
            }

            if (nextIndex !== null) {
              e.preventDefault();
              variantButtons[nextIndex].focus();
              selectVariant(option, variantButtons[nextIndex]);
            }
          });
        });

        // Initialize active variant on page load
        var activeVariant = option.querySelector('.variant-card--active');
        if (!activeVariant && variantButtons.length) {
          activeVariant = variantButtons[0];
        }

        if (activeVariant) {
          selectVariant(option, activeVariant);
        }
      });

      // Sync initially active purchase option (onetime by default) into the native selling-plan widget
      var initiallyActiveOption = optionsRoot.querySelector('.purchase-option--active') || options[0];
      if (initiallyActiveOption) {
        syncNativePurchaseOption(initiallyActiveOption.getAttribute('data-option'));
      }

      // Delivery interval dropdown -> sync to native Seal Subscriptions interval select
      var deliveryIntervalSelects = Array.prototype.slice.call(
        optionsRoot.querySelectorAll('[data-delivery-interval]')
      );

      deliveryIntervalSelects.forEach(function (select) {
        select.addEventListener('change', function () {
          syncNativeDeliveryInterval(select.value);
          updateRecommendation();
          updateAllSubscribeVariantCards();

          var subscribeOption = optionsRoot.querySelector('.purchase-option--subscribe.purchase-option--active');
          if (subscribeOption) {
            var activeVariant = subscribeOption.querySelector('.variant-card--active');
            if (activeVariant) {
              updatePrice(activeVariant, subscribeOption);
            }
          }
        });

        // Sync initial default value on page load
        syncNativeDeliveryInterval(select.value);
      });

      updateAllSubscribeVariantCards();

      // Add To Cart Event
      var addToCartButtons = optionsRoot.querySelectorAll('[data-add-to-cart]');

      addToCartButtons.forEach(function (btn) {
        btn.addEventListener('click', function (e) {
          e.preventDefault();

          const purchaseOption = btn.dataset.addToCart;
          const nativeSubmitButton = document.querySelector(
            '#ProductSubmitButton-{{ section.id }}, .product-form__submit'
          );

          if (!nativeSubmitButton) {
            console.error('Shopify Add To Cart button not found.');
            return;
          }

          if (purchaseOption === 'onetime') {
            // Select One-Time in Seal App
            syncNativePurchaseOption('onetime');

            setTimeout(function () {
              nativeSubmitButton.dispatchEvent(
                new MouseEvent('click', {
                  bubbles: true,
                  cancelable: true,
                })
              );
            }, 500);
          } else {
            // Select Subscribe in Seal App
            syncNativePurchaseOption('subscribe');

            // Get custom delivery interval
            const activePurchase = document.querySelector('.purchase-option--active');

            const customSelect = activePurchase ? activePurchase.querySelector('[data-delivery-interval]') : null;

            if (customSelect) {
              syncNativeDeliveryInterval(customSelect.value);
            }

            setTimeout(function () {
              nativeSubmitButton.dispatchEvent(
                new MouseEvent('click', {
                  bubbles: true,
                  cancelable: true,
                })
              );
            }, 600);
          }
        });
      });
    })();
  </script>
</section>

{% schema %}
{
  "name": "t:sections.main-product.name",
  "tag": "section",
  "class": "section",
  "blocks": [
    {
      "type": "@app"
    },
    {
      "type": "text",
      "name": "t:sections.main-product.blocks.text.name",
      "settings": [
        {
          "type": "inline_richtext",
          "id": "text",
          "default": "Text block",
          "label": "t:sections.main-product.blocks.text.settings.text.label"
        },
        {
          "type": "header",
          "content": "t:sections.social_media_collage.settings.color_setting"
        },
        {
          "type": "color",
          "id": "text_color",
          "label": "t:sections.main-product.blocks.text.settings.text.label",
          "default": "#666666"
        }
      ]
    },
    {
      "type": "title",
      "name": "t:sections.main-product.blocks.title.name",
      "limit": 1,
      "settings": [
        {
          "type": "header",
          "content": "t:sections.social_media_collage.settings.color_setting"
        },
        {
          "type": "color",
          "id": "product_title_color",
          "label": "t:sections.main-product.blocks.title.name",
          "default": "#131313"
        }
      ]
    },
    {
      "type": "view",
      "name": "t:sections.main-product.blocks.view-counter.name",
      "limit": 1,
      "settings": [
        {
          "type": "color",
          "id": "visitor_bg",
          "label": "t:sections.main-product.blocks.view-counter.settings.bg_color",
          "default": "#f8f7fc"
        },
        {
          "type": "color",
          "id": "visitor_text",
          "label": "t:sections.main-product.blocks.view-counter.settings.text_color",
          "default": "#333333"
        },
        {
          "type": "text",
          "id": "count_text",
          "label": "t:sections.main-product.blocks.view-counter.settings.visitor_text",
          "default": "people are viewing this right now"
        },
        {
          "type": "range",
          "id": "count_range_max",
          "label": "t:sections.main-product.blocks.view-counter.settings.max_range",
          "max": 50,
          "min": 20,
          "step": 1,
          "default": 30
        },
        {
          "type": "range",
          "id": "count_range_min",
          "label": "t:sections.main-product.blocks.view-counter.settings.min_range",
          "max": 30,
          "min": 1,
          "step": 1,
          "default": 10
        },
        {
          "type": "text",
          "id": "view_duration",
          "label": "t:sections.main-product.blocks.view-counter.settings.change_sec",
          "default": "5000",
          "info": "1000 = 1sec"
        }
      ]
    },
    {
      "type": "price",
      "name": "t:sections.main-product.blocks.price.name",
      "limit": 1,
      "settings": [
        {
          "type": "header",
          "content": "t:sections.social_media_collage.settings.color_setting"
        },
        {
          "type": "color",
          "id": "regular_price",
          "label": "t:sections.main-product.sale_price",
          "default": "#666666"
        },
        {
          "type": "color",
          "id": "sale_price",
          "label": "t:sections.main-product.regular_price",
          "default": "#131313"
        }
      ]
    },
    {
      "type": "product_sales",
      "name": "t:sections.main-product.blocks.product-sales.name",
      "limit": 1,
      "settings": [
        {
          "type": "color",
          "id": "sale_bg",
          "label": "t:sections.main-product.blocks.product-sales.settings.bg_color",
          "default": "#f8f7fc"
        },
        {
          "type": "color",
          "id": "sale_text",
          "label": "t:sections.main-product.blocks.product-sales.settings.text_color",
          "default": "#333333"
        },
        {
          "type": "range",
          "id": "minqty",
          "label": "t:sections.main-product.blocks.product-sales.settings.min_value",
          "default": 20,
          "min": 1,
          "max": 100,
          "step": 1
        },
        {
          "type": "range",
          "id": "maxqty",
          "label": "t:sections.main-product.blocks.product-sales.settings.max_value",
          "default": 30,
          "min": 1,
          "max": 100,
          "step": 1
        },
        {
          "type": "range",
          "id": "min_value",
          "label": "t:sections.main-product.blocks.product-sales.settings.min_hour",
          "default": 6,
          "min": 1,
          "max": 100,
          "step": 1
        },
        {
          "type": "range",
          "id": "max_value",
          "label": "t:sections.main-product.blocks.product-sales.settings.max_hour",
          "default": 24,
          "min": 1,
          "max": 100,
          "step": 1
        },
        {
          "type": "text",
          "id": "product-sale-timer",
          "label": "t:sections.main-product.blocks.product-sales.settings.change_num",
          "default": "5000",
          "info": "1000 = 1sec"
        },
        {
          "type": "text",
          "id": "item_sold",
          "label": "t:sections.main-product.blocks.product-sales.settings.first_text",
          "default": "Sold"
        },
        {
          "type": "text",
          "id": "products",
          "label": "t:sections.main-product.blocks.product-sales.settings.second_text",
          "default": "Product In last"
        },
        {
          "type": "text",
          "id": "hours",
          "label": "t:sections.main-product.blocks.product-sales.settings.third_text",
          "default": "Hours"
        }
      ]
    },
    {
      "type": "sku",
      "name": "t:sections.main-product.blocks.sku.name",
      "limit": 1,
      "settings": [
        {
          "type": "header",
          "content": "t:sections.social_media_collage.settings.color_setting"
        },
        {
          "type": "color",
          "id": "sku_heading",
          "label": "t:sections.main-product.sku_color",
          "default": "#131313"
        }
      ]
    },
    {
      "type": "inventory",
      "name": "t:sections.main-product.blocks.inventory.name",
      "limit": 1,
      "settings": [
        {
          "type": "paragraph",
          "content": "When location is set by app then this inventory not show."
        },
        {
          "type": "select",
          "id": "text_style",
          "options": [
            {
              "value": "body",
              "label": "t:sections.main-product.blocks.inventory.settings.text_style.options__1.label"
            },
            {
              "value": "subtitle",
              "label": "t:sections.main-product.blocks.inventory.settings.text_style.options__2.label"
            },
            {
              "value": "uppercase",
              "label": "t:sections.main-product.blocks.inventory.settings.text_style.options__3.label"
            }
          ],
          "default": "body",
          "label": "t:sections.main-product.blocks.inventory.settings.text_style.label"
        },
        {
          "type": "range",
          "id": "inventory_threshold",
          "label": "t:sections.main-product.blocks.inventory.settings.inventory_threshold.label",
          "min": 0,
          "max": 100,
          "step": 1,
          "info": "t:sections.main-product.blocks.inventory.settings.inventory_threshold.info",
          "default": 10
        },
        {
          "type": "checkbox",
          "id": "show_inventory_quantity",
          "label": "t:sections.main-product.blocks.inventory.settings.show_inventory_quantity.label",
          "default": true
        },
        {
          "type": "header",
          "content": "t:sections.social_media_collage.settings.color_setting"
        },
        {
          "type": "color",
          "id": "inventory_text",
          "label": "t:sections.main-product.text",
          "default": "#333333"
        }
      ]
    },
    {
      "type": "quantity_selector",
      "name": "t:sections.main-product.blocks.quantity_selector.name",
      "limit": 1,
      "settings": [
        {
          "type": "header",
          "content": "t:sections.social_media_collage.settings.color_setting"
        },
        {
          "type": "color",
          "id": "quantity_heading",
          "label": "t:sections.main-product.quantity_heading",
          "default": "#131313"
        }
      ]
    },
    {
      "type": "variant_picker",
      "name": "t:sections.main-product.blocks.variant_picker.name",
      "limit": 1,
      "settings": [
        {
          "type": "select",
          "id": "picker_type",
          "options": [
            {
              "value": "dropdown",
              "label": "t:sections.main-product.blocks.variant_picker.settings.picker_type.options__1.label"
            },
            {
              "value": "button",
              "label": "t:sections.main-product.blocks.variant_picker.settings.picker_type.options__2.label"
            }
          ],
          "default": "button",
          "label": "t:sections.main-product.blocks.variant_picker.settings.picker_type.label"
        },
        {
          "type": "header",
          "content": "t:sections.social_media_collage.settings.color_setting"
        },
        {
          "type": "color",
          "id": "var_heading",
          "label": "t:sections.main-product.var_heading",
          "default": "#131313"
        },
        {
          "type": "color",
          "id": "active_variant_bg",
          "label": "t:sections.main-product.active_var_bg",
          "default": "#131313"
        },
        {
          "type": "color",
          "id": "active_variant_color",
          "label": "t:sections.main-product.active_var_text",
          "default": "#ffffff"
        },
        {
          "type": "color",
          "id": "variant_bg",
          "label": "t:sections.main-product.var_bg",
          "default": "#ffffff"
        },
        {
          "type": "color",
          "id": "variant_color",
          "label": "t:sections.main-product.var_text",
          "default": "#131313"
        }
      ]
    },
    {
      "type": "buy_buttons",
      "name": "t:sections.main-product.blocks.buy_buttons.name",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_dynamic_checkout",
          "default": true,
          "label": "t:sections.main-product.blocks.buy_buttons.settings.show_dynamic_checkout.label",
          "info": "t:sections.main-product.blocks.buy_buttons.settings.show_dynamic_checkout.info"
        },
        {
          "type": "checkbox",
          "id": "show_gift_card_recipient",
          "default": true,
          "label": "t:sections.main-product.blocks.buy_buttons.settings.show_gift_card_recipient.label",
          "info": "t:sections.main-product.blocks.buy_buttons.settings.show_gift_card_recipient.info"
        },
        {
          "type": "header",
          "content": "t:sections.social_media_collage.settings.color_setting"
        },
        {
          "type": "color",
          "id": "add_btn_bg",
          "label": "t:sections.main-product.button_bg",
          "default": "#131313"
        },
        {
          "type": "color",
          "id": "add_btn_color",
          "label": "t:sections.main-product.button_text",
          "default": "#ffffff"
        },
        {
          "type": "color",
          "id": "add_btn_hr_bg",
          "label": "t:sections.main-product.button_hr_bg",
          "default": "#555540"
        },
        {
          "type": "color",
          "id": "add_btn_hr_color",
          "label": "t:sections.main-product.button_hr_text",
          "default": "#ffffff"
        },
        {
          "type": "header",
          "content": "t:sections.main-product.blocks.buy_buttons.settings.product_collection.label"
        },
        {
          "type": "checkbox",
          "id": "collection_show",
          "default": true,
          "label": "t:sections.main-product.blocks.buy_buttons.settings.product_collection.show_collection"
        },
        {
          "type": "color",
          "id": "collection_label_color",
          "default": "#333333",
          "label": "t:sections.main-product.blocks.buy_buttons.settings.product_collection.label_color"
        },
        {
          "type": "color",
          "id": "collection_color",
          "default": "#666666",
          "label": "t:sections.main-product.blocks.buy_buttons.settings.product_collection.collection_color"
        },
        {
          "type": "color",
          "id": "collection_hr_color",
          "default": "#333333",
          "label": "t:sections.main-product.blocks.buy_buttons.settings.product_collection.collection_hr_color"
        }
      ]
    },
    {
      "type": "description",
      "name": "t:sections.main-product.blocks.description.name",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_readmore",
          "label": "Show readmore button",
          "default": false
        },
        {
          "type": "text",
          "id": "button_text1",
          "label": "More button text",
          "default": "Read more"
        },
        {
          "type": "text",
          "id": "button_text2",
          "label": "Less button text",
          "default": "Read less"
        },
        {
          "type": "header",
          "content": "t:sections.social_media_collage.settings.color_setting"
        },
        {
          "type": "color",
          "id": "description_color",
          "label": "t:sections.featured-collection.settings.description.label",
          "default": "#131313"
        }
      ]
    },
    {
      "type": "share",
      "name": "t:sections.main-product.blocks.share.name",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "share_label",
          "label": "t:sections.main-product.blocks.share.settings.text.label",
          "default": "Share"
        },
        {
          "type": "paragraph",
          "content": "t:sections.main-product.blocks.share.settings.featured_image_info.content"
        },
        {
          "type": "paragraph",
          "content": "t:sections.main-product.blocks.share.settings.title_info.content"
        }
      ]
    },
    {
      "type": "custom_liquid",
      "name": "t:sections.custom-liquid.name",
      "settings": [
        {
          "type": "liquid",
          "id": "custom_liquid",
          "label": "t:sections.custom-liquid.settings.custom_liquid.label",
          "info": "t:sections.custom-liquid.settings.custom_liquid.info"
        }
      ]
    },
    {
      "type": "collapsible_tab",
      "name": "t:sections.main-product.blocks.collapsible_tab.name",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "default": "Collapsible row",
          "info": "t:sections.main-product.blocks.collapsible_tab.settings.heading.info",
          "label": "t:sections.main-product.blocks.collapsible_tab.settings.heading.label"
        },
        {
          "type": "select",
          "id": "icon",
          "options": [
            {
              "value": "none",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__1.label"
            },
            {
              "value": "apple",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__2.label"
            },
            {
              "value": "banana",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__3.label"
            },
            {
              "value": "bottle",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__4.label"
            },
            {
              "value": "box",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__5.label"
            },
            {
              "value": "carrot",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__6.label"
            },
            {
              "value": "chat_bubble",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__7.label"
            },
            {
              "value": "check_mark",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__8.label"
            },
            {
              "value": "clipboard",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__9.label"
            },
            {
              "value": "dairy",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__10.label"
            },
            {
              "value": "dairy_free",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__11.label"
            },
            {
              "value": "dryer",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__12.label"
            },
            {
              "value": "eye",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__13.label"
            },
            {
              "value": "fire",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__14.label"
            },
            {
              "value": "gluten_free",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__15.label"
            },
            {
              "value": "heart",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__16.label"
            },
            {
              "value": "iron",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__17.label"
            },
            {
              "value": "leaf",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__18.label"
            },
            {
              "value": "leather",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__19.label"
            },
            {
              "value": "lightning_bolt",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__20.label"
            },
            {
              "value": "lipstick",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__21.label"
            },
            {
              "value": "lock",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__22.label"
            },
            {
              "value": "map_pin",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__23.label"
            },
            {
              "value": "nut_free",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__24.label"
            },
            {
              "value": "pants",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__25.label"
            },
            {
              "value": "paw_print",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__26.label"
            },
            {
              "value": "pepper",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__27.label"
            },
            {
              "value": "perfume",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__28.label"
            },
            {
              "value": "plane",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__29.label"
            },
            {
              "value": "plant",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__30.label"
            },
            {
              "value": "price_tag",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__31.label"
            },
            {
              "value": "question_mark",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__32.label"
            },
            {
              "value": "recycle",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__33.label"
            },
            {
              "value": "return",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__34.label"
            },
            {
              "value": "ruler",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__35.label"
            },
            {
              "value": "serving_dish",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__36.label"
            },
            {
              "value": "shirt",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__37.label"
            },
            {
              "value": "shoe",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__38.label"
            },
            {
              "value": "silhouette",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__39.label"
            },
            {
              "value": "snowflake",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__40.label"
            },
            {
              "value": "star",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__41.label"
            },
            {
              "value": "stopwatch",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__42.label"
            },
            {
              "value": "truck",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__43.label"
            },
            {
              "value": "washing",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__44.label"
            }
          ],
          "default": "check_mark",
          "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.label"
        },
        {
          "type": "richtext",
          "id": "content",
          "label": "t:sections.main-product.blocks.collapsible_tab.settings.content.label"
        },
        {
          "type": "page",
          "id": "page",
          "label": "t:sections.main-product.blocks.collapsible_tab.settings.page.label"
        },
        {
          "type": "header",
          "content": "t:sections.social_media_collage.settings.color_setting"
        },
        {
          "type": "color",
          "id": "acco_title",
          "label": "t:sections.main-product.title_and_icon",
          "default": "#131313"
        },
        {
          "type": "color",
          "id": "acco_content",
          "label": "t:sections.main-product.blocks.collapsible_tab.settings.content.label",
          "default": "#666666"
        }
      ]
    },
    {
      "type": "comparewishlist",
      "name": "Compare and Wishlist",
      "settings": [
        {
          "type": "header",
          "content": "Compare and Wishlist"
        }
      ]
    },
    {
      "type": "popup",
      "name": "t:sections.main-product.blocks.popup.name",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "default": "Pop-up link text",
          "label": "t:sections.main-product.blocks.popup.settings.link_label.label"
        },
        {
          "id": "page",
          "type": "page",
          "label": "t:sections.main-product.blocks.popup.settings.page.label"
        },
        {
          "type": "header",
          "content": "t:sections.social_media_collage.settings.color_setting"
        },
        {
          "type": "color",
          "id": "sizechart_color",
          "label": "t:sections.main-product.text",
          "default": "#131313"
        }
      ]
    },
    {
      "type": "rating",
      "name": "t:sections.main-product.blocks.rating.name",
      "limit": 1,
      "settings": [
        {
          "type": "paragraph",
          "content": "t:sections.main-product.blocks.rating.settings.paragraph.content"
        }
      ]
    },
    {
      "type": "complementary",
      "name": "t:sections.main-product.blocks.complementary_products.name",
      "limit": 1,
      "settings": [
        {
          "type": "paragraph",
          "content": "t:sections.main-product.blocks.complementary_products.settings.paragraph.content"
        },
        {
          "type": "text",
          "id": "block_heading",
          "default": "Pairs well with",
          "label": "t:sections.main-product.blocks.complementary_products.settings.heading.label"
        },
        {
          "type": "checkbox",
          "id": "make_collapsible_row",
          "default": false,
          "label": "t:sections.main-product.blocks.complementary_products.settings.make_collapsible_row.label"
        },
        {
          "type": "select",
          "id": "icon",
          "options": [
            {
              "value": "none",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__1.label"
            },
            {
              "value": "apple",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__2.label"
            },
            {
              "value": "banana",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__3.label"
            },
            {
              "value": "bottle",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__4.label"
            },
            {
              "value": "box",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__5.label"
            },
            {
              "value": "carrot",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__6.label"
            },
            {
              "value": "chat_bubble",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__7.label"
            },
            {
              "value": "check_mark",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__8.label"
            },
            {
              "value": "clipboard",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__9.label"
            },
            {
              "value": "dairy",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__10.label"
            },
            {
              "value": "dairy_free",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__11.label"
            },
            {
              "value": "dryer",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__12.label"
            },
            {
              "value": "eye",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__13.label"
            },
            {
              "value": "fire",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__14.label"
            },
            {
              "value": "gluten_free",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__15.label"
            },
            {
              "value": "heart",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__16.label"
            },
            {
              "value": "iron",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__17.label"
            },
            {
              "value": "leaf",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__18.label"
            },
            {
              "value": "leather",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__19.label"
            },
            {
              "value": "lightning_bolt",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__20.label"
            },
            {
              "value": "lipstick",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__21.label"
            },
            {
              "value": "lock",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__22.label"
            },
            {
              "value": "map_pin",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__23.label"
            },
            {
              "value": "nut_free",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__24.label"
            },
            {
              "value": "pants",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__25.label"
            },
            {
              "value": "paw_print",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__26.label"
            },
            {
              "value": "pepper",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__27.label"
            },
            {
              "value": "perfume",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__28.label"
            },
            {
              "value": "plane",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__29.label"
            },
            {
              "value": "plant",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__30.label"
            },
            {
              "value": "price_tag",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__31.label"
            },
            {
              "value": "question_mark",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__32.label"
            },
            {
              "value": "recycle",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__33.label"
            },
            {
              "value": "return",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__34.label"
            },
            {
              "value": "ruler",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__35.label"
            },
            {
              "value": "serving_dish",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__36.label"
            },
            {
              "value": "shirt",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__37.label"
            },
            {
              "value": "shoe",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__38.label"
            },
            {
              "value": "silhouette",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__39.label"
            },
            {
              "value": "snowflake",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__40.label"
            },
            {
              "value": "star",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__41.label"
            },
            {
              "value": "stopwatch",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__42.label"
            },
            {
              "value": "truck",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__43.label"
            },
            {
              "value": "washing",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__44.label"
            }
          ],
          "default": "price_tag",
          "info": "t:sections.main-product.blocks.complementary_products.settings.icon.info",
          "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.label"
        },
        {
          "type": "range",
          "id": "product_list_limit",
          "min": 1,
          "max": 10,
          "step": 1,
          "default": 10,
          "label": "t:sections.main-product.blocks.complementary_products.settings.product_list_limit.label"
        },
        {
          "type": "range",
          "id": "products_per_page",
          "min": 1,
          "max": 4,
          "step": 1,
          "default": 3,
          "label": "t:sections.main-product.blocks.complementary_products.settings.products_per_page.label"
        },
        {
          "type": "select",
          "id": "pagination_style",
          "options": [
            {
              "value": "dots",
              "label": "t:sections.main-product.blocks.complementary_products.settings.pagination_style.options.option_1"
            },
            {
              "value": "counter",
              "label": "t:sections.main-product.blocks.complementary_products.settings.pagination_style.options.option_2"
            },
            {
              "value": "numbers",
              "label": "t:sections.main-product.blocks.complementary_products.settings.pagination_style.options.option_3"
            }
          ],
          "label": "t:sections.main-product.blocks.complementary_products.settings.pagination_style.label",
          "default": "counter"
        },
        {
          "type": "header",
          "content": "t:sections.main-product.blocks.complementary_products.settings.product_card.heading"
        },
        {
          "type": "select",
          "id": "image_ratio",
          "options": [
            {
              "value": "portrait",
              "label": "t:sections.main-product.blocks.complementary_products.settings.image_ratio.options.option_1"
            },
            {
              "value": "square",
              "label": "t:sections.main-product.blocks.complementary_products.settings.image_ratio.options.option_2"
            }
          ],
          "label": "t:sections.multicolumn.settings.image_ratio.label2",
          "default": "square"
        },
        {
          "type": "checkbox",
          "id": "enable_quick_add",
          "label": "t:sections.main-product.blocks.complementary_products.settings.enable_quick_add.label",
          "default": false
        }
      ]
    },
    {
      "type": "icon-with-text",
      "name": "t:sections.main-product.blocks.icon_with_text.name",
      "settings": [
        {
          "type": "select",
          "id": "layout",
          "options": [
            {
              "value": "horizontal",
              "label": "t:sections.main-product.blocks.icon_with_text.settings.layout.options__1.label"
            },
            {
              "value": "vertical",
              "label": "t:sections.main-product.blocks.icon_with_text.settings.layout.options__2.label"
            }
          ],
          "default": "horizontal",
          "label": "t:sections.main-product.blocks.icon_with_text.settings.layout.label"
        },
        {
          "type": "header",
          "content": "t:sections.main-product.blocks.icon_with_text.settings.content.label",
          "info": "t:sections.main-product.blocks.icon_with_text.settings.content.info"
        },
        {
          "type": "select",
          "id": "icon_1",
          "options": [
            {
              "value": "none",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__1.label"
            },
            {
              "value": "apple",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__2.label"
            },
            {
              "value": "banana",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__3.label"
            },
            {
              "value": "bottle",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__4.label"
            },
            {
              "value": "box",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__5.label"
            },
            {
              "value": "carrot",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__6.label"
            },
            {
              "value": "chat_bubble",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__7.label"
            },
            {
              "value": "check_mark",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__8.label"
            },
            {
              "value": "clipboard",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__9.label"
            },
            {
              "value": "dairy",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__10.label"
            },
            {
              "value": "dairy_free",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__11.label"
            },
            {
              "value": "dryer",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__12.label"
            },
            {
              "value": "eye",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__13.label"
            },
            {
              "value": "fire",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__14.label"
            },
            {
              "value": "gluten_free",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__15.label"
            },
            {
              "value": "heart",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__16.label"
            },
            {
              "value": "iron",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__17.label"
            },
            {
              "value": "leaf",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__18.label"
            },
            {
              "value": "leather",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__19.label"
            },
            {
              "value": "lightning_bolt",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__20.label"
            },
            {
              "value": "lipstick",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__21.label"
            },
            {
              "value": "lock",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__22.label"
            },
            {
              "value": "map_pin",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__23.label"
            },
            {
              "value": "nut_free",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__24.label"
            },
            {
              "value": "pants",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__25.label"
            },
            {
              "value": "paw_print",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__26.label"
            },
            {
              "value": "pepper",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__27.label"
            },
            {
              "value": "perfume",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__28.label"
            },
            {
              "value": "plane",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__29.label"
            },
            {
              "value": "plant",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__30.label"
            },
            {
              "value": "price_tag",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__31.label"
            },
            {
              "value": "question_mark",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__32.label"
            },
            {
              "value": "recycle",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__33.label"
            },
            {
              "value": "return",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__34.label"
            },
            {
              "value": "ruler",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__35.label"
            },
            {
              "value": "serving_dish",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__36.label"
            },
            {
              "value": "shirt",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__37.label"
            },
            {
              "value": "shoe",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__38.label"
            },
            {
              "value": "silhouette",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__39.label"
            },
            {
              "value": "snowflake",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__40.label"
            },
            {
              "value": "star",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__41.label"
            },
            {
              "value": "stopwatch",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__42.label"
            },
            {
              "value": "truck",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__43.label"
            },
            {
              "value": "washing",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__44.label"
            }
          ],
          "default": "heart",
          "label": "t:sections.main-product.blocks.icon_with_text.settings.icon_1.label"
        },
        {
          "type": "image_picker",
          "id": "image_1",
          "label": "t:sections.main-product.blocks.icon_with_text.settings.image_1.label"
        },
        {
          "type": "inline_richtext",
          "id": "heading_1",
          "default": "Heading",
          "label": "t:sections.main-product.blocks.icon_with_text.settings.heading_1.label",
          "info": "t:sections.main-product.blocks.icon_with_text.settings.heading.info"
        },
        {
          "type": "select",
          "id": "icon_2",
          "options": [
            {
              "value": "none",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__1.label"
            },
            {
              "value": "apple",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__2.label"
            },
            {
              "value": "banana",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__3.label"
            },
            {
              "value": "bottle",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__4.label"
            },
            {
              "value": "box",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__5.label"
            },
            {
              "value": "carrot",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__6.label"
            },
            {
              "value": "chat_bubble",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__7.label"
            },
            {
              "value": "check_mark",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__8.label"
            },
            {
              "value": "clipboard",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__9.label"
            },
            {
              "value": "dairy",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__10.label"
            },
            {
              "value": "dairy_free",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__11.label"
            },
            {
              "value": "dryer",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__12.label"
            },
            {
              "value": "eye",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__13.label"
            },
            {
              "value": "fire",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__14.label"
            },
            {
              "value": "gluten_free",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__15.label"
            },
            {
              "value": "heart",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__16.label"
            },
            {
              "value": "iron",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__17.label"
            },
            {
              "value": "leaf",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__18.label"
            },
            {
              "value": "leather",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__19.label"
            },
            {
              "value": "lightning_bolt",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__20.label"
            },
            {
              "value": "lipstick",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__21.label"
            },
            {
              "value": "lock",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__22.label"
            },
            {
              "value": "map_pin",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__23.label"
            },
            {
              "value": "nut_free",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__24.label"
            },
            {
              "value": "pants",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__25.label"
            },
            {
              "value": "paw_print",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__26.label"
            },
            {
              "value": "pepper",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__27.label"
            },
            {
              "value": "perfume",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__28.label"
            },
            {
              "value": "plane",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__29.label"
            },
            {
              "value": "plant",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__30.label"
            },
            {
              "value": "price_tag",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__31.label"
            },
            {
              "value": "question_mark",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__32.label"
            },
            {
              "value": "recycle",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__33.label"
            },
            {
              "value": "return",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__34.label"
            },
            {
              "value": "ruler",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__35.label"
            },
            {
              "value": "serving_dish",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__36.label"
            },
            {
              "value": "shirt",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__37.label"
            },
            {
              "value": "shoe",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__38.label"
            },
            {
              "value": "silhouette",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__39.label"
            },
            {
              "value": "snowflake",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__40.label"
            },
            {
              "value": "star",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__41.label"
            },
            {
              "value": "stopwatch",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__42.label"
            },
            {
              "value": "truck",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__43.label"
            },
            {
              "value": "washing",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__44.label"
            }
          ],
          "default": "return",
          "label": "t:sections.main-product.blocks.icon_with_text.settings.icon_2.label"
        },
        {
          "type": "image_picker",
          "id": "image_2",
          "label": "t:sections.main-product.blocks.icon_with_text.settings.image_2.label"
        },
        {
          "type": "inline_richtext",
          "id": "heading_2",
          "default": "Heading",
          "label": "t:sections.main-product.blocks.icon_with_text.settings.heading_2.label",
          "info": "t:sections.main-product.blocks.icon_with_text.settings.heading.info"
        },
        {
          "type": "select",
          "id": "icon_3",
          "options": [
            {
              "value": "none",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__1.label"
            },
            {
              "value": "apple",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__2.label"
            },
            {
              "value": "banana",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__3.label"
            },
            {
              "value": "bottle",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__4.label"
            },
            {
              "value": "box",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__5.label"
            },
            {
              "value": "carrot",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__6.label"
            },
            {
              "value": "chat_bubble",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__7.label"
            },
            {
              "value": "check_mark",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__8.label"
            },
            {
              "value": "clipboard",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__9.label"
            },
            {
              "value": "dairy",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__10.label"
            },
            {
              "value": "dairy_free",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__11.label"
            },
            {
              "value": "dryer",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__12.label"
            },
            {
              "value": "eye",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__13.label"
            },
            {
              "value": "fire",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__14.label"
            },
            {
              "value": "gluten_free",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__15.label"
            },
            {
              "value": "heart",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__16.label"
            },
            {
              "value": "iron",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__17.label"
            },
            {
              "value": "leaf",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__18.label"
            },
            {
              "value": "leather",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__19.label"
            },
            {
              "value": "lightning_bolt",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__20.label"
            },
            {
              "value": "lipstick",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__21.label"
            },
            {
              "value": "lock",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__22.label"
            },
            {
              "value": "map_pin",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__23.label"
            },
            {
              "value": "nut_free",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__24.label"
            },
            {
              "value": "pants",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__25.label"
            },
            {
              "value": "paw_print",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__26.label"
            },
            {
              "value": "pepper",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__27.label"
            },
            {
              "value": "perfume",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__28.label"
            },
            {
              "value": "plane",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__29.label"
            },
            {
              "value": "plant",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__30.label"
            },
            {
              "value": "price_tag",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__31.label"
            },
            {
              "value": "question_mark",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__32.label"
            },
            {
              "value": "recycle",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__33.label"
            },
            {
              "value": "return",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__34.label"
            },
            {
              "value": "ruler",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__35.label"
            },
            {
              "value": "serving_dish",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__36.label"
            },
            {
              "value": "shirt",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__37.label"
            },
            {
              "value": "shoe",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__38.label"
            },
            {
              "value": "silhouette",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__39.label"
            },
            {
              "value": "snowflake",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__40.label"
            },
            {
              "value": "star",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__41.label"
            },
            {
              "value": "stopwatch",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__42.label"
            },
            {
              "value": "truck",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__43.label"
            },
            {
              "value": "washing",
              "label": "t:sections.main-product.blocks.collapsible_tab.settings.icon.options__44.label"
            }
          ],
          "default": "truck",
          "label": "t:sections.main-product.blocks.icon_with_text.settings.icon_3.label"
        },
        {
          "type": "image_picker",
          "id": "image_3",
          "label": "t:sections.main-product.blocks.icon_with_text.settings.image_3.label"
        },
        {
          "type": "inline_richtext",
          "id": "heading_3",
          "default": "Heading",
          "label": "t:sections.main-product.blocks.icon_with_text.settings.heading_3.label",
          "info": "t:sections.main-product.blocks.icon_with_text.settings.heading.info"
        },
        {
          "type": "header",
          "content": "t:sections.social_media_collage.settings.color_setting"
        },
        {
          "type": "color",
          "id": "icon_block_text",
          "label": "t:sections.main-product.text",
          "default": "#131313"
        }
      ]
    }
  ],
  "settings": [
    {
      "type": "checkbox",
      "id": "show_stick_cart",
      "default": true,
      "label": "Show sticky cart"
    },
    {
      "type": "color",
      "id": "sticky_bg",
      "default": "#ffffff",
      "label": "Sticky cart background color"
    },
    {
      "type": "checkbox",
      "id": "enable_sticky_info",
      "default": true,
      "label": "t:sections.main-product.settings.enable_sticky_info.label"
    },
    {
      "type": "header",
      "content": "t:sections.main-product.settings.header.content",
      "info": "t:sections.main-product.settings.header.info"
    },
    {
      "type": "select",
      "id": "media_size",
      "options": [
        {
          "value": "small",
          "label": "t:sections.main-product.settings.media_size.options__1.label"
        },
        {
          "value": "medium",
          "label": "t:sections.main-product.settings.media_size.options__2.label"
        },
        {
          "value": "large",
          "label": "t:sections.main-product.settings.media_size.options__3.label"
        }
      ],
      "default": "large",
      "label": "t:sections.main-product.settings.media_size.label",
      "info": "t:sections.main-product.settings.media_size.info"
    },
    {
      "type": "checkbox",
      "id": "constrain_to_viewport",
      "default": true,
      "label": "t:sections.main-product.settings.constrain_to_viewport.label"
    },
    {
      "type": "select",
      "id": "media_fit",
      "options": [
        {
          "value": "contain",
          "label": "t:sections.main-product.settings.media_fit.options__1.label"
        },
        {
          "value": "cover",
          "label": "t:sections.main-product.settings.media_fit.options__2.label"
        }
      ],
      "default": "contain",
      "label": "t:sections.main-product.settings.media_fit.label"
    },
    {
      "type": "select",
      "id": "gallery_layout",
      "options": [
        {
          "value": "stacked",
          "label": "t:sections.main-product.settings.gallery_layout.options__1.label"
        },
        {
          "value": "columns",
          "label": "t:sections.main-product.settings.gallery_layout.options__2.label"
        },
        {
          "value": "thumbnail",
          "label": "t:sections.main-product.settings.gallery_layout.options__3.label"
        },
        {
          "value": "thumbnail_slider",
          "label": "t:sections.main-product.settings.gallery_layout.options__4.label"
        },
        {
          "value": "thumbnail_slider_verticle",
          "label": "Thumbnail left verticle slider"
        },
        {
          "value": "thumbnail_right_slider_verticle",
          "label": "Thumbnail right verticle slider"
        },
        {
          "value": "product_sticky",
          "label": "Thumbnail sticky"
        }
      ],
      "default": "stacked",
      "label": "t:sections.main-product.settings.gallery_layout.label"
    },
    {
      "type": "select",
      "id": "media_position",
      "options": [
        {
          "value": "left",
          "label": "t:sections.main-product.settings.media_position.options__1.label"
        },
        {
          "value": "right",
          "label": "t:sections.main-product.settings.media_position.options__2.label"
        }
      ],
      "default": "left",
      "label": "t:sections.main-product.settings.media_position.label",
      "info": "t:sections.main-product.settings.media_position.info"
    },
    {
      "type": "select",
      "id": "image_zoom",
      "options": [
        {
          "value": "lightbox",
          "label": "t:sections.main-product.settings.image_zoom.options__1.label"
        },
        {
          "value": "hover",
          "label": "t:sections.main-product.settings.image_zoom.options__2.label"
        },
        {
          "value": "none",
          "label": "t:sections.main-product.settings.image_zoom.options__3.label"
        }
      ],
      "default": "lightbox",
      "label": "t:sections.main-product.settings.image_zoom.label",
      "info": "t:sections.main-product.settings.image_zoom.info"
    },
    {
      "type": "select",
      "id": "mobile_thumbnails",
      "options": [
        {
          "value": "columns",
          "label": "t:sections.main-product.settings.mobile_thumbnails.options__1.label"
        },
        {
          "value": "show",
          "label": "t:sections.main-product.settings.mobile_thumbnails.options__2.label"
        },
        {
          "value": "hide",
          "label": "t:sections.main-product.settings.mobile_thumbnails.options__3.label"
        }
      ],
      "default": "hide",
      "label": "t:sections.main-product.settings.mobile_thumbnails.label"
    },
    {
      "type": "checkbox",
      "id": "hide_variants",
      "default": false,
      "label": "t:sections.main-product.settings.hide_variants.label"
    },
    {
      "type": "checkbox",
      "id": "enable_video_looping",
      "default": false,
      "label": "t:sections.main-product.settings.enable_video_looping.label"
    },
    {
      "type": "header",
      "content": "t:sections.social_media_collage.settings.color_setting"
    },
    {
      "type": "color",
      "id": "border_color",
      "label": "t:sections.social_media_collage.settings.border_color",
      "default": "#e5e5e5"
    },
    {
      "type": "header",
      "content": "t:sections.main-product.pickup_availibility"
    },
    {
      "type": "color",
      "id": "drawer_bg",
      "label": "t:sections.main-product.drawer_bg",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "text_heading_color",
      "label": "t:sections.main-product.text_heading",
      "default": "#131313"
    },
    {
      "type": "color",
      "id": "text_description_color",
      "label": "t:sections.main-product.text_description",
      "default": "#666666"
    },
    {
      "type": "header",
      "content": "Section padding"
    },
    {
      "type": "number",
      "id": "padding-top",
      "label": "Padding top",
      "default": 60
    },
    {
      "type": "number",
      "id": "padding-bottom",
      "label": "Padding bottom",
      "default": 60
    },
    {
      "type": "header",
      "content": "Screen wise section padding"
    },
    {
      "type": "header",
      "content": "@media screen 1429"
    },
    {
      "type": "number",
      "id": "padding-top-1429",
      "label": "Padding top",
      "default": 50
    },
    {
      "type": "number",
      "id": "padding-bottom-1429",
      "label": "Padding bottom",
      "default": 50
    },
    {
      "type": "header",
      "content": "@media screen 1199"
    },
    {
      "type": "number",
      "id": "padding-top-1199",
      "label": "Padding top",
      "default": 45
    },
    {
      "type": "number",
      "id": "padding-bottom-1199",
      "label": "Padding bottom",
      "default": 45
    },
    {
      "type": "header",
      "content": "@media screen 991"
    },
    {
      "type": "number",
      "id": "padding-top-991",
      "label": "Padding top",
      "default": 40
    },
    {
      "type": "number",
      "id": "padding-bottom-991",
      "label": "Padding bottom",
      "default": 40
    },
    {
      "type": "header",
      "content": "@media screen 767"
    },
    {
      "type": "number",
      "id": "padding-top-767",
      "label": "Padding top",
      "default": 35
    },
    {
      "type": "number",
      "id": "padding-bottom-767",
      "label": "Padding bottom",
      "default": 35
    },
    {
      "type": "header",
      "content": "@media screen 575"
    },
    {
      "type": "number",
      "id": "padding-top-575",
      "label": "Padding top",
      "default": 30
    },
    {
      "type": "number",
      "id": "padding-bottom-575",
      "label": "Padding bottom",
      "default": 30
    }
  ]
}
{% endschema %}

<!-- bcpo done -->